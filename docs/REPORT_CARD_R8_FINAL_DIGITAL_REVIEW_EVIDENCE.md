# Report-Card R8 Final Digital Review Evidence

**Scope:** Classes I-X only; synthetic evidence; digital design approved; physical acceptance pending

R8 is the final narrow table-priority correction for `NALANDA_LEGACY_REFINED_COLOUR` and `NALANDA_LEGACY_REFINED_MONOCHROME`. It preserves the approved header, identity grid, calculation snapshot, grade scales, result-state codes, chart data, colour palette and true-monochrome patterns.

## Corrected geometry

- Each enabled result metric is a measured, single-line bordered cell such as `Total: 547.8 / 600`; three-, four- and five-cell variants omit disabled metrics and redistribute width without wrapping, ellipses or hidden values.
- `DENSE_MARKS_TABLE_PRIORITY` activates for row, result-column, grouping or projected-width pressure. Classes VI-X combine Personality Development and Grade into one readable column so the academic table receives 74% of the available table width in the synthetic review geometry.
- Governed `NORMAL_CHART`, `COMPACT_CHART` and `COMPACT_DENSE_CHART` modes shrink chart padding/footprint only when table density requires it. Class IX Combined retains the two-row fallback; standard VI-X reports use the one-row fallback only where complete labels and collision-safe values remain readable.
- Attendance and General Remarks share a compact aligned 44/56 row.
- All four equal signature columns retain 15.0 mm clear handwriting space, with the line above the label and 4.6 mm line-to-label separation.

## Local-only outputs

- `.codex/report-print-accept-1a/r8/FINAL-DIGITAL-REVIEW-R8.pdf` - eight representative Classes I-X pages in matched colour/true-monochrome pairs.
- `.codex/report-print-accept-1a/r8/R8-DETAIL-CHECKS.pdf` - six compact measured proofs.
- `.codex/report-print-accept-1a/r8/final-print-pack/PHYSICAL-ACCEPTANCE-CLASSES-I-X-COLOUR.pdf` - paused eight-page Classes I-X-only candidate.
- `.codex/report-print-accept-1a/r8/final-print-pack/PHYSICAL-ACCEPTANCE-CLASSES-I-X-MONOCHROME.pdf` - paused true-monochrome equivalent.
- The same ignored folder contains print instructions and the specimen manifest. These packs must not be printed until the user approves the final digital review.

All 30 generated pages were rasterized at 300 DPI and inspected programmatically and visually. The checks cover A4 geometry, nonblank pages, outer clipping bands, white canvases, true monochrome, table and chart legibility, summary no-wrap behavior, pattern equivalence and signature clearance. No KG page, historical source page, real Student data or real report publication is present.

## Gate

R8 received normal-language final digital approval on 14 August 2026 and is recorded as `R8_DIGITAL_DESIGN_APPROVED`. The verified Classes I-X-only packs may proceed to synthetic physical printing under `docs/REPORT_CARD_R8_DIGITAL_APPROVAL_AND_PHYSICAL_GATE.md`. The branch remains unmerged and untagged; no deployment, real report publication or physical acceptance has occurred. KG/LKG/UKG remains preserved/default-off under `IMPLEMENTED_FOUNDATION_DEFERRED_TO_V1_5`.
