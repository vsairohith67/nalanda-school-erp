# Release recovery 1C — engineering checkpoint, 2026-09-20

**RELEASE_RECOVERY_1C_PARTIAL_WITH_NAMED_GAPS**

This checkpoint starts at `a2180b6e8b073ba84f61c494d44e84d13a77c373`, tree `4d64b07e635d7fcf29b0a8c80213ef7ea4264190`, on retained PR28 / `release/recovery-integration-1a`. Released main remains `104aacc7bd314cae82e60bb02b5c8a965c7ffedd`. The earlier `73d8dbd` baseline and all admitted PR24–27 source history remain reachable. OCR PR19 remains excluded. No application, dependency, database-schema or migration pin/version changed. The integrated backup contract remains `NALANDA_RECOVERY_INTEGRATED:v48:certificates-concessions-items`.

## Concrete changes and limits

- Removed latent public `oci-layout/` and `oci-release/` uploads. They were behind disabled jobs; no new disclosure was observed. Exact allowlisted projections contain source identifiers, hashes and result metadata only. Native private-only guards remain. No historical artifact was deleted.
- Extended the existing genuine-exporter recovery suite to independent A/B targets for every v45/v46/v47/v48 contract and both providers. Each target is checked empty except exact migration-authored reference rows, restored fresh, and restored again. Independent sentinel writes, source snapshots, financial sums, artifact bytes/hashes, ownership, history and provider/session safety are asserted. A separate mapped-account probe verifies restored sessions are revoked; it is not counted as another fresh restore. Failed cases cannot emit passed matrix rows.
- Added raw-report/receipt validation binding source, current input hashes, architecture, pinned bases, configuration digest, architecture manifest/index digests, SBOM, scan reports, scanner/database metadata and provenance. Artificial descriptors are labelled `HARNESS_FIXTURE_ONLY` and cannot qualify runtime. Missing, malformed, stale, conflicting, suppressed High/Critical and substituted evidence fails closed. No editable pass boolean grants admission.
- Moved future stack execution onto each architecture's image runner. Compose has no application build directive and consumes the admitted configuration digest with pulls disabled. Exact infrastructure pins are preloaded and scanned separately. Full running-image checks reject tag substitution and application-code mounts. Independent workflow teardown is retained alongside harness `finally`; repeated cleanup verifies absence rather than reconstructing removed secrets.
- Corrected a proven latent workflow defect: inherited Trivy action commit `a9c7b0f06e461e9d4b4d1711f154ee024b8d7ab8` returns upstream HTTP422/no commit. The intended upstream **v0.36.0** resolves to `ed142fd0673e97e23eac54620cfb913e5ce36c25`; that exact commit is now pinned. Severity thresholds were not lowered. [Upstream action](https://github.com/aquasecurity/trivy-action/tree/ed142fd0673e97e23eac54620cfb913e5ce36c25).
- The public CLI parser/dispatcher is exercised across all ten commands through bounded in-process adapters, including failures and durable resume. A real CLI subprocess denial test proves no target creation before qualification. A separate real-process runner implements a subset of deployed lifecycle scenarios and explicitly fails completion while its pending scenarios remain.
- Added an authenticated production-OFF HTTP driver, explicit transactional synthetic fixture preparation, actual login, preview receipt and flag-denial assertions. Authoritative readback executes a bundled read-only transaction inside every admitted serving replica, including certificate requests/sequences/events, charges, item snapshots and prior-year adjustments. It verifies the actual web process's startup database secret, pinned proxy/PG images, exact owned networks, unique upstream/PG aliases, committed proxy configuration and stable target identity. A separate host database cannot establish this evidence. The future same-runner stack calls `--run-off` after TLS/readiness. **This code is compiled/source-tested but not runtime-executed.** Discovery records ON/browser/native scenarios as pending, not passing.

## Newly executed local evidence

`tmp/recovery-1c/restore-sqlite-v4.log`: four contract tests pass, **8 independent fresh SQLite restores +8 repeats**. Every contract v45, v46, v47 and v48 has distinct A/B files, with source and sibling isolation proven. Collision, ownership omission, atomic interruption, valid retry, unknown/fingerprint/version rejection, missing collection and altered artifact checks remain. Genuine historical exporters and migration checksums are preserved. Business/security tables are empty before each initial restore; SQLite's seeded reference counts are IamSafetyLock1, SupportQueue8, SupportCategoryPolicy18 and OperationalCheckDefinition13. PostgreSQL runs through the existing hosted financial workflow after push; no local PostgreSQL service or unqualified historical server was started.

Focused publication/artifact/operator/discovery/release-state checks: **132 passed**. Broader retained register/portable/feature-flag regressions: **193 passed across13 files** (includes those132). Affected tools/tests TypeScript partitions and portable source bundle passed before this checkpoint. Twelve fresh package audits (root plus three workspace packages; all/prod/dev separately) returned zero findings. Public-source/private-artifact scan passed. YAML parsed for all workflows, the seven canonical portable job IDs are preserved, PowerShell parsed, and whitespace checks are required before commit. Final exact-head full regression/build/database/native source workflows remain a post-push gate; this document does not predeclare their outcome.

Earlier failed local restore attempts are retained: exact migration-seeded baseline correction; missing local user/session mapping expectation correction; Prisma Decimal JSON-transport cloning correction. No production restore code, validation check or historical migration was weakened to fix these harness assumptions.

One independent read-only reviewer examined publication bypasses, raw-report semantics, image substitution, receipt timing and source/target isolation. Findings corrected include ARG-based Dockerfile pins, admitted image propagation, supported Compose flags, suppressed findings, full running-container inspection, infrastructure preload, independent teardown and HTTP hostname/CA prerequisites. This is source review, not runtime qualification.

Post-checkpoint source review found the initial HTTP driver could compare a separately supplied host database with the serving application's effects. The bounded follow-up repair above removes that connection entirely and adds 19 target-binding tests (proxy substitution, foreign/shadowed aliases, differing database networks, stopped/duplicate replicas, writable/foreign proxy files, host resolution overrides). Seventy focused artifact/publication/operator/HTTP discovery and binding tests passed across five files; affected TypeScript and portable bundling are required again. The original `72a7d2e` hosted matrix already passed all16 fresh+16 repeated restorations; final-head workflows will produce separate receipts. Three Windows jobs on that earlier checkpoint encountered vendor libsodium DNS error11002; historical failures remain, with only failed jobs retried where requested before this source defect was found. This is a justified source-repair push, not a documentation-only rerun.

## Mandatory coverage mapping and remaining work

| Existing group | Location after change | Execution / limitation |
|---|---|---|
| Server/database | `server-and-database` | Existing full source tests/build retained |
| OCI/native supply chain | each native `oci-image` runner | Hard disabled; no image built/scanned here |
| Multiplatform index | `oci-release-index` | Explicit metadata aggregate only; no deployable OCI index retained |
| Portable stack | `qa-stack.ps1` inside image runner; `portable-stack` verifies both receipts | Same-runner orchestration implemented; NOT_EXECUTED |
| Distributed/resilience/offline/shared client | `distributed-runtime` | Existing commands retained; runtime-dependent skip remains mandatory blocker |
| Object/recovery | `object-storage-and-recovery` | Existing commands retained; runtime-dependent skip remains mandatory blocker |
| Full synthetic / focused products | `full-synthetic-acceptance` | Existing commands retained; source builds are not substituted for the scanned app |

| Type | Remaining prerequisite / concrete action | Required proof | More code? |
|---|---|---|---|
| Engineering | Finish the distinct independent-target public-CLI object restore, initialisation target, deployed interruption/resume and data/key readbacks | Real CLI subprocesses, separate empty target, authoritative contents, bounded failures, verified teardown | Yes |
| Engineering | Reconcile immutable PostgreSQL production artifact with the existing SQLite-only synthetic QA-ON mechanism | Narrow independently reviewed test profile; production remains OFF; no auth bypass | Yes |
| Engineering | Complete PR25/26 ON HTTP fixture/actions and wire retained PR27 flows; finish actual browser/native drivers | All required business state/audit/download/payload/focus scenarios; no mocked success | Yes |
| Engineering/privacy | Resolve inherited onboarding happy-path contact fixtures without plausible real numbers or weakening real-contact validation | Valid synthetic fixture design; public scanner and real validation both pass | Yes / reviewed fixture design |
| External runtime | Preserve 1B's unresolved vendor OS/bundled-Node findings; no new vendor experiment in 1C | Supported exact amd64/arm64 artifacts, current zero-unresolved-Critical/High scans, native/SBOM/provenance evidence | Conditional |
| Historical runtime | Distinct qualified historical/current image pair with compatible schema/backup contracts | Actual upgrade and compatible rollback; incompatible rollback refusal | Qualification plus harness completion |
| Runtime acceptance | Execute completed harnesses only after artifact/dependency admission | Authenticated OFF/ON HTTP, browser and actual operator evidence; cleanup readback | Execution plus above fixes |
| Owner / environment | Approved private artifact retention and staged/pilot/cutover/privacy decisions; required installed Windows and physical Android/iPhone/iPad access, signing where required | Separate evidence for packaging, PIN launch, authenticated installed app, physical devices; owner decisions recorded | No substitute source test |

No new requirement is marked complete. Canonical release readiness still requires exactly one PASSED result per mandatory and additional gate. Expected private-publication skips remain separate from mandatory runtime skips. Compilation/PIN evidence is not installed/authenticated/physical-device proof.

## What is now ready when the runtime qualifies?

The existing `Portable Staging Foundation exact-head` workflow contains the future same-runner sequence: build, inspect, two scans, native probe, `scanner-metadata.mjs`, `capture-artifact-evidence.ts`, `qa-stack.ps1`, independent cleanup, metadata projection. **Do not dispatch it expecting acceptance until the reviewed hard hold and engineering gaps above are resolved.** `admit-artifact.ts` retains a non-overridable `EXTERNAL_RUNTIME_BLOCKED` exception.

Safe source commands now: `pnpm exec vitest run tests/portable-artifact-handoff.test.ts tests/portable-publication-1c.test.ts tests/portable-operator-cli-1c.test.ts tests/portable-acceptance-discovery.test.ts`; `pnpm exec tsx scripts/portable/operator-acceptance.ts --discover`; `pnpm exec tsx scripts/portable/integrated-acceptance.ts --discover`. Discovery is not acceptance. The existing financial workflow runs the real provider restore matrix and uploads only its exact sanitised summary file. No synthetic fixture can clear real artifact qualification.

## Safety and final readback

Before-work operational DB file hash matched `65F47EFA37DA321023439303770645F8D656F2BE58458C1A03B341408EF9A6FA`, size8409088, mtime2026-08-10T10:55:19.8897824Z; no WAL/SHM/journal. The pre-push read-only hash/size/mtime and absence of sidecars match again; final live refs are required in the terminal report. No SQL connection to that file. All25 flags remain OFF/0%; all46 requirement IDs, owner corrections and migrations remain unchanged.

No application server, image build or container stack was started in this pass. Synthetic local fixtures/logs remain under ignored task-owned paths. The old policy-denied recovery residue and five other-task PR27 packages were untouched. No merge, tag, original PR closure, registry, purchase, deployment, real contact/record processing, communication, payment, OCR/model/provider activation or production telemetry occurred.

Final SHA/tree, terminal run/job links, hosted PostgreSQL matrix, elapsed time, DB readback and narrow tracker results belong in `tmp/recovery-1c/final-report.md` and PR28's dated delta after CI. Tracker failures require an explicit unapplied patch. This checkpoint is intentionally partial; it does not claim all independently requested harness code is finished.

## 2026-09-20 — RELEASE-RECOVERY-1D source closure delta

Starting source `e221bc91293db910daa4670721e6c4e4d14d5d9c`, tree `018d32114c9bd4e5b654fe2d2e54c3daf299f49b`. This dated section extends the existing closure ledger. The 1C evidence above remains historical. No migration, schema, backup contract, requirement ID or operational flag is changed. Production/QA are distinct image targets; no new image has been built or admitted in 1D.

**Candidate remains PARTIAL_WITH_EXACT_CODE_GAPS.** Executable source has advanced in all four packages, but the remaining engineering below is not a vendor/device prerequisite. Neither a successful source test nor the runtime admission guard makes these packages complete.

| Package / defect | Implementation and meaningful permitted evidence | Implementation | Execution | Runtime prerequisite / remaining code |
|---|---|---|---|---|
| A project isolation and public lifecycle | `operator-acceptance.ts`, `operator-adapter.ts`, `operator.ts`, `operator-interruption.ts`: distinct projects, credentials, volumes, target ownership, actual public subprocesses, dry-run/repeat receipts, process-group interruption and stale-lock validation. Operator process/filesystem tests reject ambiguous effects and unknown/blank writers. | PARTIAL | CONTRACT_TESTED | Qualified exact current/historical images; full deployed scenarios have not run. |
| A encrypted independent recovery | `recovery-handoff.ts`, `operator-recovery.ts`, `recovery-readback.ts`: ciphertext/manifest/provenance/key verification, empty target before writes, destination-owned object storage, actual financial/certificate/ownership/reversal readback, wrong-key/corrupt/missing/mismatched inputs, data/object/key-preserving uninstall. Cryptographic and filesystem refusals pass. | PARTIAL | CONTRACT_TESTED | Private historical income-data key custody remains missing, described below. |
| A genuine rich fixture | `operator-fixture.ts`, `synthetic-foundation.ts`, the existing restore matrix: genuine v48 exporter output, required nonempty certificate/item/concession/payment-attribution history, hash/source/run receipt; shared supported profile initialisation after restore. Existing four-contract SQLite test completed four cases, eight fresh targets and eight repeats; exporter validation and wrong-hash/source refusals passed. | PARTIAL | CONTRACT_TESTED | Same-runner fixture production/path handoff is not wired into `qa-stack.ps1`; never substitute an old or publicly uploaded fixture. |
| B PostgreSQL QA boundary | `synthetic-capability.ts` (library and host signer), `prepare-synthetic-build.mjs`, `synthetic-compose.ts`, Dockerfile: separately labelled optimized PostgreSQL test build, Ed25519 run/build/container/database/origin/expiry binding, read-only capabilities, real auth/MFA, production null trust and unchanged OFF flags. Copied/stale/tampered/wrong-target/missing proof tests pass. | PARTIAL | CONTRACT_TESTED | Complete the same-runner test-build/scan/admission/private CA/font/startup/teardown orchestration. Compiled production-null-trust image verification remains unexecuted. |
| B private onboarding contacts | `private-fixtures.ts`, reused bulk driver, `onboarding.ts`: private RAM generation under isolated Linux/no-default-route checks; no assertion that generated numbers are reserved or owned. Country-code parsing now still validates the national number. No contact delivery, public fixture activation API or login bypass. Validator/privacy contract tests pass. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED | Actual isolated serving-container execution and no-egress observation still required. |
| C real HTTP subset | `acceptance-http.ts`, `integrated-bulk.ts`, `integrated-business.ts`, `integrated-on.ts`, reused PR27 driver: password plus real TOTP login/step-up; challenge/session/grant database readback; actual bulk flows, certificate preview/charge/issue/reprint/reissue/void, item receipts/rate freezing, prior-year waiver/application/concurrency/reversal. Same-serving-database binding retained. | PARTIAL | CONTRACT_TESTED | Remaining authority/privacy scenarios below; admitted separate test image and genuine font/CA. No integrated HTTP result is claimed. |
| C actual browser subset | `integrated-browser.ts`, `browser-probe.ts`: actual login, six viewport/theme combinations, projected Student upload payload, preview/confirm/readback, CSV bytes, file cancellation, keyboard/zoom/reduced-motion and console failure handling. Projection/download bound tests pass. | PARTIAL | CONTRACT_TESTED | Modal/Escape/focus restoration, error-view reflow, continuous private-flash observation and rendered business flows remain source work. Browser rendering is NOT_EXECUTED. |
| D native subset | `native-webdriver.ts`: real W3C commands for tauri-driver/EdgeDriver or Appium contexts, PIN unlock, real system-browser login/MFA and original PKCE link, reference refresh, secure lock and draft UI actions. Endpoint/session/protocol/context negative tests pass. | PARTIAL | CONTRACT_TESTED | Owned platform session/driver orchestration, callback coordination, scoped test profile, offline/reconnect/readback, revocation/rotation and file handling remain code. No native authenticated execution. |

### Exact remaining engineering

1. **Historical private data keys:** the genuine fixture encrypts income under a test key version that differs from each new project's authentication key. Preserving ciphertext alone is insufficient. Add separately mounted private historical-data-key custody using reviewed key-version collision/rotation handling, without replacing destination active authentication keys. Verify an authorized exact-income read. `assertRecoveryPrivacyKeys` now authenticates encrypted income before database/object writes and fails with `RECOVERY_PRIVATE_DATA_KEY_CUSTODY_REQUIRED`; tests cover valid, wrong and missing keys. The operator driver remains ineligible while custody is incomplete.
2. **Operator fixture orchestration:** generate the genuine private fixture on the admitted OCI runner and pass its same-head/run/attempt root to `PORTABLE_OPERATOR_FIXTURE_ROOT`; add owned cleanup for this new fixture. No public fixture archive or catalogue insertion. Historical inputs must supply the compatible operator recovery command contract as well as compatible v48 schema; older images without it cannot execute these helpers.
3. **QA host orchestration:** connect the separately identified test build to same-run native/security scans and artifact capture, isolated setup, licensed Georgia Bold mount, real CA trust, capability issue, execution and guaranteed verified teardown. Helpers exist; this complete caller does not. No workflow hold was removed.
4. **HTTP coverage:** Parent-child certificate downloads and QR privacy, stale/revoked concession authority, restricted/exact income and sponsorship-not-received checks, full fee/Cash Book invariance and per-operation audit assertions still need executable actions/assertions. Reused PR27 HTTP checks do not prove these missing cases or integrated rendering.
5. **Browser coverage:** actual certificate/item/concession/receipt/PDF/QR and governed/legacy marks UI flows; mapping/modal focus/Escape/return; stale bundle/authority reset; long errors at 320px while errors remain visible; continuous unauthorised private-data flash observation. The current subset's reset-view width and single Tab checks do not satisfy these requirements.
6. **Native coverage:** executable owned Windows/Appium session setup/teardown and private package/profile identity, actual system-browser callback coordination and approved device authority, native/offline feature capability scope, disconnected persistence, reconnect/idempotency/conflict authoritative readback, expiry/revocation/rotation, file-picker/download and protected-screen acceptance. These are source gaps, separate from required tools/devices/signing decisions.

### Entrypoints, evidence classes and preservation

Permitted contract commands: focused `tests/portable-operator*`, `portable-recovery-handoff-1d`, `portable-synthetic-capability-1d`, `portable-synthetic-compose-1d`, `private-fixtures-1d`, `portable-browser-driver-1d`, `native-driver-1d`; unchanged full `recovery-backup-compatibility.test.ts` on both providers; TypeScript partitions and `node scripts/portable/bundle.mjs`. Process-adapter evidence is explicitly not deployed CLI proof.

Future gated commands: `pnpm exec tsx scripts/portable/operator-acceptance.ts` with private same-run fixture and distinct qualified historical/current artifacts; existing `integrated-acceptance.ts --run-off <absolute-private-fixture> <full-container-id>`; `synthetic-capability.ts prepare-build` / `issue <full-container-id>`; `synthetic-compose.ts`; `integrated-acceptance.ts --run-on <full-container-id>`. The ON entrypoint still fails completion after its executable subset; missing scenarios are retained. Native helpers have no complete orchestration entrypoint yet. Playwright/Chromium with actual private CA trust, tauri-driver/EdgeDriver, Appium UiAutomator2/XCUITest, appropriate owned devices and separately admitted private packages remain execution inputs.

Independent read-only review checked operator separation/encrypted recovery, QA trust boundary, fixtures, auth/step-up, same-serving DB, actual driver actions and classifications. It found and prompted fixes for unknown writers, doctor ordering, transient native status, MFA session evidence, browser error privacy and unreadable historical income. The remaining key-custody and orchestration findings are deliberately open.

Production defaults remain OFF/0%; test build evidence can never represent identical production-image acceptance. Package locks are unchanged; fresh root/native/bridge/migrator all/prod/dev audits reported zero findings in all 12 reports. No OS/bundled-runtime finding was waived or re-scanned. No image/package publication, deletion, runtime launch, merge, tag, real contact processing, payment, provider activation or operational database connection occurred. Final exact-head CI/job states, new versus reused provider evidence, protected-file hash/readback, tracker results and elapsed time belong in `tmp/recovery-1d/final-report.md`; missing or skipped gates remain ineligible.

### 1D final source follow-up: custody and executable scenario corrections

This follow-up supersedes the corresponding open entries immediately above; it does not relabel earlier source evidence or integrated execution.

- **A custody implemented and reviewed:** `recovery-key-custody.ts` transfers only historical versions referenced by real income envelopes, binds the private custody file to the fixture/source/run/attempt, refuses version collisions, and preserves each destination active authentication key. The generator now gives projects distinct key-version IDs. Real secret mounts are present before first startup. Prewrite decryption and persisted-row decryption preserve original ciphertext. Wrong/missing/key-collision/case-AAD/provenance/expected-value refusals have meaningful contract tests. This is cryptographic recovery proof, not an authorized HTTP income read.
- **A fixture caller implemented and reviewed:** `prepare-operator-fixture.ts` runs the unchanged four-contract, two-target-per-provider matrix on same-run owned SQLite files, captures genuine nonempty v48 output and a private random historical keyring using an exact locator, runs the public operator driver, and validates ownership before removing only its newly created files. `qa-stack.ps1` now invokes this caller. Operator required scenarios derive missing evidence from actual action receipts, including restored data/key/object preservation; final historical status is taken from validated operator evidence. The new local matrix passed eight fresh restores and eight repeats. No deployed operator execution occurred.
- **A remaining orchestration:** the hosted workflow still needs a supported private producer/supply path for `artifact-evidence-history`, including a distinct qualified ancestor image with compatible public recovery commands and matching schema/migration contract. The consumer accepts and validates those inputs; the producer is not implemented. This is an engineering gap separate from vendor runtime qualification.
- **B isolated caller implemented:** `qa-synthetic-stack.ts` consumes separately admitted production/QA image identities, scans inherited infrastructure into phase-specific evidence, starts a distinct `...-qaon` owned project, mounts genuine Georgia Bold, issues per-replica capabilities, trusts only the private CA in an isolated browser NSS home, executes the integrated driver and always invokes verified owned teardown. `ciProject` accepts exactly production-OFF or synthetic-ON; it is resource naming, never feature authorization. Existing admission receipts are checked before creating a target. No TLS waiver or operational flag change.
- **B remaining orchestration:** the same-run QA image build/scan/capture producer and capability-signing-root cleanup are still not wired into hosted workflow execution. Audited Playwright/Chromium and certutil installation are explicit inputs. The caller cannot manufacture admission or make the incomplete integrated suite pass.
- **C HTTP corrections reviewed:** Parent with a different linked child is denied the target PDF, while own-child PDF bytes match the authoritative hash. A real MFA-authenticated Accountant with general-income ALLOW and exact-income DENY exercises the finer privacy boundary; exact-view audit counts remain unchanged on refusal. Expired/revoked step-up grants cannot approve or create concession events. Sponsorship promise approval cannot become relief or received money. Nonzero current-year fees/payment preconditions plus canonical allocation and complete row digests survive waiver application/reversal.
- **C Browser corrections:** the real import driver keeps parsed errors and long unmapped headers visible during the 320px reflow check. A DOM observer records any protected heading/canary appearing throughout initial unauthenticated navigation. No mocked HTTP response or synthetic rendered result is used.

Current implementation/execution classification:

| Package | Implementation | Permitted evidence | Remaining source work / runtime prerequisites |
|---|---|---|---|
| A consumer and private custody | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; fresh SQLite 8+8 restores | Historical artifact producer remains PARTIAL. Admitted current and historical native images required; deployed lifecycle NOT_EXECUTED. |
| B feature boundary and isolated caller | PARTIAL | CONTRACT_TESTED for capabilities, phase isolation and Compose; caller typechecked/reviewed | Same-run QA build/scan/capture and signing-root destruction/readback; qualified distinct QA artifact, licensed font, certutil and audited Browser runtime. |
| C authenticated HTTP/Browser | PARTIAL | Existing authoritative service tests, source/type checks and focused helper contracts | QR decode/privacy status, revoked role-assignment/stale authority coverage and full per-operation audit coverage; rendered certificate/item/concession/receipt/marks/download flows, modal/Escape/focus return, stale bundle/authority and continuous denied-role observation. Integrated HTTP/rendered execution NOT_EXECUTED. |
| D native | PARTIAL | Existing W3C/shared-client contract tests | Owned platform/session/driver and private artifact-profile orchestration, callback/deep-link coordination, offline/reconnect/idempotency/conflict readback, session lifecycle and file picker/download. Actual authenticated Windows/Android/iPhone/iPad execution NOT_EXECUTED. |

Entrypoints added: `pnpm exec tsx scripts/portable/prepare-operator-fixture.ts` and `pnpm exec tsx scripts/portable/qa-synthetic-stack.ts`. Both retain mandatory artifact admission. The ON suite continues to fail completion explicitly until missing scenarios are implemented; no pending list was silently emptied. Source checkpoint f06ab53 completed nine successful workflows, including 269 files / 2,566 tests with zero test skips and both-provider 16 fresh + 16 repeated restores; that checkpoint is historical after these corrections. Final candidate coordinates and exact-head terminal evidence are recorded in the local final report. Overall status remains RELEASE_RECOVERY_1D_PARTIAL_WITH_EXACT_CODE_GAPS.

### 20 September 2026 — RECOVERY-QA-ARTIFACT-PRODUCER-1A

This bounded delta closes only the Package B same-run QA producer and signing-root lifecycle. Starting source: `85df8bb8cd6d0d8e130b11632d2e046c35028a20`, tree `ae6d1aec2c8b676d9d27a586179201a936945e40`. Historical evidence and all A/C/D gaps above remain; no full Package B execution or release clearance is inferred.

| Closure item | Implementation and meaningful test | Implementation | Execution |
|---|---|---|---|
| Source to separate QA artifact | `qa-artifact-producer.ts`, `qa-artifact-producer-cli.ts`, `qa-build-tools.ts`, `qa-rootless-build.ts`: exact committed archive, production admission before preparation, native architecture, pinned public vendor tools, fresh rootless BuildKit storage/socket, synthetic target, public trust only, config-digest addressing throughout scans/probe/export. Command propagation/order and changed input/artifact refusal tests. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED |
| Capture to admission to existing caller | Existing `capture-artifact-evidence.ts` and `admit-artifact.ts` bind raw same-run scans/native/OCI provenance, tool pins and trust; `qa-synthetic-stack.ts` consumes that evidence. Malformed scan, High finding, stale/substituted artifact and admission denial prevent caller execution. Harness fixtures cannot qualify runtime. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED |
| Signing and teardown | `synthetic-build-lifecycle.ts`, `producer-process.ts`: fresh Ed25519 root, source/run/attempt/architecture/phase/project/target ownership, bounded private files, key/public correspondence, ancestor/link checks, catchable cancellation and finally cleanup; independent workflow cleanup checks removal. Every reachable stage failure and foreign-root refusal covered. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED |

Actual entrypoints are `pnpm exec tsx scripts/portable/qa-artifact-producer-cli.ts run` and independent `... cleanup`. The existing OCI architecture job invokes both and uploads only exact allowlisted sanitised metadata. Production-OFF acceptance and its receipt remain independently eligible after a QA caller failure, provided production admission and QA cleanup succeeded. Mandatory failures are not converted to success.

BuildKit v0.33.0 and RootlessKit v3.2.0 official amd64/arm64 archives are checksum-pinned in `config/qa-build-tools.json`; no tool or application image was downloaded/built/executed for this task. Vendor interface review used the pinned BuildKit source, including the JSON-array disk-usage format. The future runner must support an unprivileged user namespace, subordinate IDs, normal process sandbox and native snapshotter; unsupported runners fail preflight, without kernel/AppArmor changes, privileged containers or a shared-cache prune. Workflows install uidmap, certutil and Chromium system dependencies. Playwright 1.63.0 is pinned in the existing workspace lockfile; browser binaries stay in the newly owned private work root.

The signer is outside the committed-source build context. Private keys never enter build arguments, layers, caches, logs or public artifacts. Only the existing public trust mechanism enters the separate QA target; the ordinary production trust remains null. Real authentication/MFA/step-up/scope checks are unchanged. QA artifact identity cannot satisfy identical-production-image acceptance.

Cleanup verifies exact ownership before deleting new roots or labelled resources; a foreign/symlinked/mismatched root is retained as a refusal. Builder startup intent without a valid process identity retains the work root for reconciliation. A known daemon is bound to PID plus kernel start time and stopped before filesystem cleanup. An uncatchable process/runner termination cannot promise in-process cleanup: the independent workflow cleanup attempts only proven owned resources; ambiguous live process/mount/permission residue requires disposable-runner teardown and is not reported removed. Removal is not forensic secure erasure. Previously denied historical residue and all existing artifacts remain untouched.

New local evidence: 107 focused tests in five files PASS, no test skips; affected tools-core and tests-m-r TypeScript partitions PASS; portable bundle PASS; frozen install (scripts disabled) PASS; all/production/development dependency audit views report zero findings; Git safety/publication guards PASS. External build/scanner/caller commands were simulated by explicitly labelled `HARNESS_FIXTURE_ONLY` adapters. Real Ed25519, filesystem and harmless child-process behavior were tested. Windows cancellation/timeout ran locally; the POSIX descendant assertion runs only on Linux and is not claimed as Windows evidence. One independent read-only reviewer traced the complete connection and verified corrections for descendant cancellation, owned cache, interrupted daemon startup, nested preparation cancellation and independent production receipt upload.

Hosted QA image production, actual scan/native probe, admission, application caller and deployed teardown remain NOT_EXECUTED under unchanged `EXTERNAL_RUNTIME_BLOCKED`. Runtime qualification and admission of production plus distinct QA bytes on each architecture, genuine licensed Georgia Bold, isolated private CA/network/services and existing caller prerequisites remain required. The caller still fails completeness for its explicit C/D source gaps; that is not hidden by producer success. No historical producer, business/browser/native feature work, schema/migration/v48/flag/register change, merge, tag, publication or activation belongs to this delta. Exact final source/CI/tracker coordinates are recorded in `tmp/qa-artifact-producer-1a/final-report.md` after the consolidated push. The previously refused final report-seal/integrity command is not retried; its previous protected-file baseline is reused, not presented as fresh verification.

### 23 September 2026 — RECOVERY-HTTP-PRIVACY-ACCEPTANCE-1A

Starting source `a469a46e6a677ac07655f5beb0ac3da1a9c49fce`, tree `169154246f590a6cac887eb859638f1afdd35557`. This delta closes only the three HTTP code gaps in the Package C row above. Historical evidence stays intact; no Browser/native/historical-image/producer work is included.

| Gap and executable scenario IDs | Implementation and meaningful evidence | Implementation | Execution |
|---|---|---|---|
| H1: `H1-pdf-qr-reference-authenticated-status-exact-response-privacy` | `integrated-business.ts` downloads the issued PDF; `certificate-qr.ts` rasterises its bounded 1–4 pages locally and requires exactly one decoded private URN. The token hash must match the retained artifact before authenticated verification. Exact ISSUED/SUPERSEDED/VOID/UNAVAILABLE bodies, private headers, altered/unknown/malformed references, anonymous/Parent denial, privacy sentinels and unchanged scoped domain state are asserted. Process-adapter tests cover later-page, duplicate/absent/external QR, tool failure and private cleanup. Real verification route/service projection is tested with synthetic storage adapters. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; actual PDF/QR and HTTP NOT_EXECUTED |
| H2: `H2-parent-link-revoked-stale-session-download-restored`, `H2-role-assignment-ended-valid-preview-stale-session-and-fresh-low-role`, `H2-exact-delegation-revoked-expired-valid-confirm` | Real positive controls precede exact denials. The public IAM END_ROLE route removes the Principal grant, retaining only a nonprivileged VIEWER base role; stale session401 and fresh low-role403 use an unconsumed valid preview. Existing delegated revoke/expiry/linked-child scenarios are strengthened with positive draft confirmation, current CSV/preview, exact denial body and unchanged marks/audit state. Parent-link removal/restoration and expiry are explicitly out-of-band fixture transitions. Existing step-up refusals now require exact409 PRIOR_YEAR_STEP_UP_REQUIRED. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; authenticated HTTP NOT_EXECUTED |
| H3: `H3-certificate-per-operation-actor-target-transition-idempotent-effects`, `H3-concession-per-operation-audit-denied-income-idempotent-relief-sponsorship-refusal` | `http-assertions.ts` validates exact scoped event deltas and rejects wrong actor/target/action/request, missing events and duplicate business effects. Every certificate request/review/approval/issue/void and concession transition checks the actual event contract. Charge preparation/approval/collection use retained actor/state/receipt-line proof; reprint, linked Graduation reissue and verification intentionally create no domain event. Reads/retries are separate from idempotent domain effects. Artifact/version/receipt snapshots include original and successor records. Denied income/sponsorship/authority requests cannot add successful business events. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; authenticated HTTP NOT_EXECUTED |

Existing IDs retained and strengthened: `delegation_revoked_before_commit_no_marks_change`, `delegation_expired_before_commit_no_marks_change`, `linked_child_added_after_preview_refused`, `revoked_actor_rejected`, and all previous certificate/concession business checks. The old `remaining-business-authority-privacy-scenarios` discovery disposition is replaced by the six executable IDs above. `integrated-bulk.ts` requires all six receipts before emitting its HTTP subset report. `integrated-on.ts` still reports mandatoryCoverage PARTIAL and throws INTEGRATED_REQUIRED_SCENARIOS_INCOMPLETE. No source/contract receipt can qualify runtime.

Entrypoints remain the admitted `integrated-on.ts` caller and bundled `dist/portable/integrated-bulk.mjs`; no new public endpoint or authentication bypass. The same-serving-container startup database, proxy/replica/network/secret binding is reused and readbacks recheck it. Unit adapters and route-handler invocation are UNIT_OR_CONTRACT and SERVICE_OR_ROUTE_HANDLER, not ADMITTED_FULL_STACK_HTTP. Actual HTTP requires runtime-qualified immutable production and separate QA artifacts, existing signed capability/target admission, private synthetic services/CA, real MFA/step-up, licensed Georgia Bold and an admitted environment providing local `pdftoppm` and `zbarimg`. Decoder binaries were not installed or executed here; the retained minimal image does not yet supply them. Missing tools fail explicitly, never manufacture a decoded result. No external QR service, OCR or URL navigation occurs; temporary PDFs/rasters stay private and are removed with verified ownership, without claiming forensic erasure.

Test-first helpers initially failed because the implementation was missing. New tests reject additional response fields, wrong denial reason/success, wrong audit actor/target/action, missing/duplicate effects and failed target binding. One focused read-only reviewer traced the actual routes/services and required later-page QR handling, expanded immutable snapshots, live verification denials, exact step-up refusal and precise family/delegation audits; all findings were corrected and re-reviewed. No application policy/schema defect was changed. Local test totals, final SHA/tree, exact-head CI/job links and tracker readbacks are recorded in `tmp/http-privacy-acceptance-1a/final-report.md` after source handoff.

EXTERNAL_RUNTIME_BLOCKED, Browser/native/historical producer gaps, v48, all migration checksums, 46 requirement IDs, 25 OFF/0% operational flags and public artifact guards are unchanged. Protected operational DB is untouched; its earlier fingerprint is reused, not a fresh hash. The previously refused combined integrity/report command was not retried or split. No image build, server launch, merge/tag/deployment, activation, package publication or historical cleanup occurred.
### 23 September 2026 — RECOVERY-CERTIFICATE-BROWSER-ACCEPTANCE-1A

This dated closure delta covers only the certificate Browser slice. Starting source `f4cf0f7966dbffef235e024ebd8a45c6ce38cd6c`, tree `db60339693fbdafa30b4b6109476c03589e7113b`. Historical entries below remain evidence of their original state. The completed producer, H1/H2/H3, same-serving-database binding and restoration machinery are reused unchanged. The overall integrated suite still refuses completion.

| Scenario | Actual screen and executable assertion | Implementation | Execution now |
|---|---|---|---|
| CB1-readiness-request-independent-review | `/certificates/graduation`: Student/year readiness, missing-year refusal, school recognition without inferred Board success, create request, Start Certificate Review, distinct reviewer approval; self-approval and stale confirmation denied with exact response and unchanged scoped graph. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; real isolated service transitions; rendered NOT_EXECUTED |
| CB2-charge-approval-single-collection | Same request workspace: Prepare configured charge, Inspect charge, distinct finance approval, Collect approved amount once; actor, approved amount, one receipt/line, stale-tab retry and refresh invariance. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; real service charge/receipt/retry; rendered NOT_EXECUTED |
| CB3-draft-submit-approve-issue | Approved template/date/place and Prepare draft; `/certificates/<id>` Submit Certificate for Approval, independent Approve Certificate, Issue Certificate. Exact actors/events/state/version/artifact, no self-approval, no duplicate issue under retained confirmation. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; real service draft/approval/issue/retry; rendered NOT_EXECUTED |
| CB4-private-document-reprint-qr | Preview DRAFT PDF / Reprint saved PDF listeners precede real clicks. Bounded bytes, immutable hash, actual embedded Georgia Bold/header, per-page unofficial watermark, Student/year/disclaimer; existing local QR decoder compares rendered reference hash. No URN navigation. | IMPLEMENTED_REVIEWED | Real service-generated PDF content inspected; download orchestration/missing-tool negatives CONTRACT_TESTED; actual Browser download and QR decode NOT_EXECUTED |
| CB5-governed-reissue-void | Certificate detail Reissue Certificate creates linked draft, then complete successor issue; Cancel Issued Certificate requires reason; workspace VOID history and A4 Print Preview agree; original version/artifact/receipt retained. | IMPLEMENTED_REVIEWED | CONTRACT_TESTED; real service successor/void; rendered NOT_EXECUTED |
| CB6-parent-denied-and-revoked-link | `/parent/certificates?student=<id>` own-child Open Issued Certificate / Open saved issued PDF. Controlled labelled relationship removal after positive access, same-session download refusal, restoration; other Parent/Teacher redirect to `/unauthorized`, anonymous to `/login`, continuous private-canary observer. | IMPLEMENTED_REVIEWED | Fixture ownership/relationship and driver contracts tested; real authenticated navigation/download/revocation NOT_EXECUTED |
| CB7-modal-keyboard-responsive-private-render | Actual request/review/issue/reissue/cancel dialogs: initial focus, Tab containment, Escape, trigger focus return, uncommitted cancellation, errors and stale-state closure. Six independent journeys: 1366x768,390x844,320x844 in light/dark; default motion in light and reduced motion in dark, actual theme/computed-motion/overflow/overlay/console checks. | IMPLEMENTED_REVIEWED | Simulated Page orchestration and React markup contracts only; rendered matrix NOT_EXECUTED; viewport reflow is not true Browser zoom certification |

Implementation: `scripts/portable/certificate-browser.ts`, `certificate-browser-probe.ts`, `certificate-browser-assertions.ts`, `certificate-browser-document.ts`, connected through existing `integrated-on.ts` and stdin-only `browser-probe.ts certificate`. The existing admitted entrypoint remains `pnpm exec tsx scripts/portable/integrated-acceptance.ts --run-on <full-container-id>` through the QA caller. There is no separate bypass entrypoint. Each matrix iteration has unique source/run/attempt/iteration-bound actors, Student and template, empty initial business state, and exact serving-container readbacks before/after actions. Two Principals and two Admin finance actors are narrowed by explicit DENYs to required existing permissions; no blanket Super Admin. Login uses real password/TOTP controls and persisted session/MFA evidence. Parent and low-role contexts are separate. No business outcome is inserted by fixtures.

Two narrow UI defects were corrected: existing certificate forms now reuse the existing native accessible review dialog so modal errors remain visible and stale authoritative state closes an obsolete confirmation; the Graduation A4 page now labels a cancelled record VOID instead of DRAFT. Existing global modal keyboard handling is preserved; it was not absent. Regression tests retain role/aria-modal/focus and no window-dialog assertions, following the reused component. No new screen, permission, schema or business rule.

Focused evidence: 122 tests across ten files PASS, zero skips, including the real isolated SQLite service journey. New tests reject foreign source/target/actor, missing/duplicate/wrong events and effects, changed PDF bytes/content/font, wrong denial/success response, missing control and interrupted context cleanup. The service harness stubs admission and MFA setup and therefore cannot establish runtime admission or successful Browser login. Five additional local service-document probes passed, including a genuine two-page draft watermark and wrong Student/mode rejection; no PDF or font file was published. The sole independent read-only reviewer traced actual component labels/routes, least-privilege preparation, readback, document content and caller connections; final disposition IMPLEMENTED_REVIEWED, no remaining known source gap in this slice.

Execution prerequisites remain: qualified same-run immutable production and distinct QA artifacts, existing signed capability/target admission on each serving replica, isolated hosted services/private verified TLS and CA, real Georgia Bold mount, pinned Playwright/Chromium, and separately admitted host `pdftoppm`/`zbarimg` on PATH for the existing bounded QR command. No decoder binary is installed into the serving image. Owned contexts and downloads close/delete in finally paths; private run fixtures remain for existing verified project teardown. No screenshots, private PDFs, tokens, raw errors or fixture values are published; no arbitrary external network is permitted. Missing tools or failed cleanup fail the driver, never manufacture success.

`EXTERNAL_RUNTIME_BLOCKED`, production OFF/0%, v48, all 46 IDs, public publication protections and complete-mandatory-evidence release refusal remain. Other Browser/native/historical source work is unchanged and is not called complete. No local application server, image build, merge, tag, provider activation or protected operational-file access occurred. The owner-prohibited integrity command was not retried: prior operational fingerprint is reused only, not freshly verified. Final source/CI/security and tracker readbacks are recorded in `tmp/certificate-browser-acceptance-1a/final-report.md`; skipped runtime jobs are not PASS.

Certificate-slice CI correction: the initial `bc7b883` Windows shared-platform full suite exposed a 60-second setup timeout in the new isolated SQLite service test. Its PostgreSQL counterpart passed. The SQLite fixture now executes every unchanged active migration in a fresh in-memory SQLite database, checks integrity and foreign keys, then snapshots once to its new run-owned file; it avoids repeated durable DDL flushes. The timeout and every service assertion remain unchanged. This is service-fixture preparation, not a claim of Prisma migration-runner acceptance; the existing migration and 16-fresh/16-repeat gates are unchanged. All 19 directly affected tests pass after correction; the same independent reviewer approved this narrow setup change. A necessary corrective source push supersedes the initial candidate, preserving all failed-run history. Final-head regression results belong to the handoff report.

### 24 September 2026 — RECOVERY-FINANCE-BROWSER-ACCEPTANCE-1A

Starting source `65da17efd8fb0d914aef0109c42c51317e5a21fe`, tree `daee9f5f32e8378995337cc2cbf63afe31e75c8e`. This delta covers only Student-item and previous-year-concession Browser code. Requirement links: NPS-REQ-010 and NPS-REQ-015; optional restricted case support and sponsorship-promise boundaries intersect NPS-REQ-014/016 without completing those wider requirements. All 46 IDs/history remain unchanged.

| Scenario ID | Existing screen, real action and independent assertion | Implementation | Execution |
|---|---|---|---|
| FB1-student-item-sale-validation | `/misc-income` → Create receipt → `/misc-income/new`: exact Student/year, designated item, quantity, approved dated rate; Save and issue receipt. Missing Student, invalid quantities, wrong/stale enrolment and missing rate fail without a receipt. Exact actor/Student/year/frozen lines: 3 × 10.25 = 30.75. | IMPLEMENTED_REVIEWED | UNIT_OR_DRIVER_CONTRACT + ISOLATED_SERVICE; rendered NOT_EXECUTED |
| FB2-rate-history-receipt-print | Items & rates → `/misc-income/items`: Add academic-year rate; second sale 3 × 12.50 = 37.50 preserves first snapshot. `/misc-income/<id>` → Print receipt opens actual HTML print page; bounded markup, exact row/amount/identity, genuine platform Georgia Bold, refresh/reprint and abandoned sale/cancellation leave collections unchanged. No PDF button or ordinary-sale idempotency is invented. | IMPLEMENTED_REVIEWED | Contract/service; actual popup/font/render NOT_EXECUTED |
| FB3-previous-year-source-preparation | `/prior-year-concessions`, Previous-year outstanding: real source/preparation controls and distinct source verifier. Reject current/future/older/unverified years, wrong identity, unreconciled source and excess relief. Verified 1,000 − received 200 − approved credits 50 = 750. | IMPLEMENTED_REVIEWED | Contract/service; rendered NOT_EXECUTED |
| FB4-independent-relief-reversal | Review and decision: submit/review/approve/apply, review reversal and compensating reversal, separate narrowed actors and real MFA/step-up dialog. Self-approval denied. Exact events/actors/request/version/reversal link: 750 → 450 → 750; current-year 3,600/100/3,500 and global discount unchanged; waiver creates no Payment/receipt/Cash Book entry. Immediate reversal is covered; dependent-transaction reversal refusal is not new evidence. | IMPLEMENTED_REVIEWED | Contract/service; rendered NOT_EXECUTED |
| FB5-stale-retry-sponsorship | Actual stale-tab approval and changed-balance application refusals. Run-local Chromium response-stage interruption preserves the committed APPLY; Recover original decision resends the exact actor-bound request and creates one relief event. Sponsorship is prepared/reviewed/approved but cannot apply as received money. Additional received payment is labelled fixture precondition; attribution is performed through UI. | IMPLEMENTED_REVIEWED | Deterministic CDP orchestration + real service retry/stale/sponsorship; deployed interruption NOT_EXECUTED |
| FB6-restricted-income-denied-authority | Restricted income support: UNKNOWN/NOT_PROVIDED/DECLINED/explicit zero/private value. Separate general/exact permissions, exact allowed response fields and case-selection clearing; continuous DOM/response canaries. Actual CSV bytes must match exact allowed fields and selected case; denied export/read cannot leak. Positive reviewer followed by actual role-assignment revocation and valid next request; Parent/Teacher/Viewer/anonymous follow existing policy, including legitimate Viewer receipt access. | IMPLEMENTED_REVIEWED | Contract/service + isolated component regression; real authenticated privacy NOT_EXECUTED |
| FB7-modal-responsive-offline-refusal | Existing step-up dialog: initial focus/Tab/Escape/exact focus return, cancellation and stale errors. Six unique journeys at 1366×768,390×844,320×844 light/dark, default/reduced motion, long reasons/errors, actual theme/overflow/console/overlay checks. Offline decision emits no request or queued mutation and preserves browser storage. | IMPLEMENTED_REVIEWED | Driver/component contracts; rendered matrix NOT_EXECUTED; viewport reflow is not true zoom certification |

Connection: `integrated-on.ts` invokes `financeBrowser` in `finance-browser.ts` after the retained certificate slice. `browser-probe.ts finance` invokes `finance-browser-probe.ts` through the existing signed-run-bound stdin-only serving-container connection. `finance-browser-assertions.ts` checks exact scoped graphs and per-operation events with constant private diagnostics. The existing entrypoint remains `pnpm exec tsx scripts/portable/integrated-acceptance.ts --run-on <full-container-id>` through the admitted QA caller; no new bypass entrypoint. The broader PARTIAL report and final incomplete-scenarios refusal remain intact.

Each matrix iteration owns distinct source/run/attempt/iteration-bound actors, Students, enrolments, dates and cases. Only preconditions are seeded. Real password/TOTP controls and persisted session/MFA proof remain required; fixture role grants are narrowly permitted/denied, never blanket Super Admin. Readbacks validate the serving target before/after each scoped database query. Expected cents are independent integer arithmetic. Private generated income canaries, HTTP bodies, credentials, downloads and state remain in private memory/stdin; only sanitised scenario outcomes may leave the harness. Network routes permit only the bound verified-TLS origin. Contexts, popup pages, downloads and CDP interception close in owned finally paths; ambiguous cleanup fails rather than claiming removal. Fixture records belong to existing disposable-project teardown.

Three narrow existing-interface corrections: case selection clears prior exact income synchronously; a known rejected decision refreshes authoritative state before releasing controls, preventing stale approval from remaining enabled; the existing HTML receipt school header explicitly uses Georgia bold. Regression tests exercise the real component handlers/print markup. No finance engine, permission, schema, migration, backup or global theme change.

New focused evidence: 69 tests across six files passed, no skips, including real isolated SQLite finance transitions. Tests reject wrong actor/year/Student/quantity/rate/amount, missing/duplicate effect, stale version, incorrect reversal, changed current-year figures, income disclosure, wrong denial, failed download, missing control and interrupted cleanup. Service adapters stub runtime admission and MFA preparation and are explicitly HARNESS_FIXTURE_ONLY; they cannot qualify an image or prove Browser login. Independent read-only review traced screens, actual role/step-up paths, source/target binding, readback, privacy, CDP failure handling and caller completeness. Exact final validation/CI and tracker readbacks are retained in `tmp/finance-browser-acceptance-1a/final-report.md`.

Execution requires qualified immutable production and distinct QA artifacts, signed capabilities and serving-target admission, isolated hosted services/private verified TLS, pinned Playwright/Chromium with CDP and licensed genuine Georgia Bold available to the Browser host. None was launched or qualified here. CDP response interruption plus route guarding, actual HTML print/font rendering, downloaded CSV, real login/MFA, six viewport journeys and cleanup remain rendered NOT_EXECUTED. No screenshots or private artifact publication. Other marks/native/historical work remains unchanged, and `EXTERNAL_RUNTIME_BLOCKED` plus all mandatory release gates remain. All 25 operational flags remain OFF/0%; v48, migration checksums, feature ancestry and OCR exclusion are preserved. The protected operational file was not accessed; prior fingerprint is reused only and the refused integrity command was not retried or split. No merge/tag/deployment/provider activation, historical deletion or denied-residue cleanup.


### 24 September 2026 — RECOVERY-MARKS-BROWSER-ACCEPTANCE-1A

Starting source `2c852a3c5c03b2d6df29d69ac6dad97deaa0f7fc`, tree `c2f763075ec300c2bae0a839ba977a2edb9dc9ff`. This finite slice concerns the existing EXAM1/EXAM2/academic-integrity and PR27 marks/import/report contracts, with historical academic-year invariance linked to NPS-REQ-002. No requirement disposition or any of the 46 IDs is changed.

| Scenario ID | Actual screen/action and independent assertion | Implementation | Execution |
|---|---|---|---|
| MB1 | `/marks` → Import CSV → `/marks/import`; `/marks/governed`: exact context, maximum/precision/frozen scheme and assignment; real selected-context CSV/XLSX downloads, exact eligible roster and text admission numbers; change only permitted entry fields, preserve signed context. Reject wrong model/columns/context and unsafe workbook structures. | IMPLEMENTED_REVIEWED | UNIT/DRIVER_CONTRACT + ISOLATED_SERVICE; rendered NOT_EXECUTED |
| MB2 | Legacy server preview → reviewed marks → native confirmation → draft save/reload. Positive/zero/ABSENT/EXEMPT/NOT_APPLICABLE and invalid blank/max/precision/duplicate/year/roster. Exact actor/Student/assessment/events and unchanged governed/complement records. | IMPLEMENTED_REVIEWED | UNIT/DRIVER_CONTRACT + ISOLATED_SERVICE; rendered NOT_EXECUTED |
| MB3 | Governed CSV and XLSX selected templates → local reconstruction → actual server preview/confirmation/grid reload. Exact DRAFT sheet, row/optimistic versions and DRAFT_SAVED actor; NOT_ENTERED remains distinct. Other assignment/legacy/historical published records unchanged. | IMPLEMENTED_REVIEWED | UNIT/DRIVER_CONTRACT + ISOLATED_SERVICE; rendered NOT_EXECUTED |
| MB4 | Actual file/context clearing, same-file reselection, delayed real response/navigation, genuine 15-minute expiry, separate-session valid grid edit then stale refusal. CDP drops only an observed successful confirm response; retry uses its original receipt and must produce no second authoritative effect. A separate actual Final submit establishes a read-only target for stale confirmation; this controlled action is not attributed to import. | IMPLEMENTED_REVIEWED | Contract/service retry/concurrency/state tests; actual expiry/CDP/Browser NOT_EXECUTED |
| MB5 | Real password/TOTP and session evidence; narrow Principal and exact-scope eligible delegates. Positive preview before revoked/expired/linked-child refusal; wrong assignment and Teacher/Parent/Viewer/ungranted operator/anonymous denial. Constant diagnostics, continuous DOM/network canaries, exact no-business-effect and separately expected family-security event. Authority changes are labelled controlled fixture preparation, not governance-UI coverage. | IMPLEMENTED_REVIEWED | Contract + real isolated IAM/service evidence; authenticated rendering NOT_EXECUTED |
| MB6 | `/marks/reports`: actual year/exam filters and Formula-Safe CSV, independently expected rows and empty result; positive export then permission revocation with stale control. `/academic-reports`: actual historical filters and Generate governed report, Safe CSV, Colour PDF, Monochrome PDF. Exact source versions, independent values, private headers, format/mode-specific stored audit, genuine Georgia Bold school header and unchanged publication snapshots. | IMPLEMENTED_REVIEWED | Service-generated PDF bytes and report sources verified; real downloaded bytes NOT_EXECUTED |
| MB7 | Native import dialog focus/Tab/containment/Escape/focus return/cancellation/error; six isolated 1366×768,390×844,320×844 light/dark journeys with default/reduced motion, theme, wrapping/overflow/overlay/console checks. Real offline request failure leaves no stored/queued marks operation or automatic replay. | IMPLEMENTED_REVIEWED | Driver/component contracts; rendered NOT_EXECUTED; VIEWPORT_REFLOW_ONLY, not true zoom certification |

Connection: `integrated-on.ts` invokes `marksBrowser` after retained certificate/finance slices. `browser-probe.ts marks` delegates to `marks-browser-probe.ts` over existing signed stdin-only serving-container readback. `marks-browser-history.ts` prepares only explicit nonempty historical publication preconditions; it never seeds the imported drafts or generated report/export under test. Assertions and bounded document inspection live in `marks-browser-assertions.ts` and `marks-browser-document.ts`. The existing admitted entrypoint remains `pnpm exec tsx scripts/portable/integrated-acceptance.ts --run-on <full-container-id>`, reached by the QA caller; no bypass entrypoint. Per-iteration source/run/attempt ownership, real authentication, private TLS/origin and exact serving-image/database binding remain mandatory. Contexts/CDP/downloads close in owned finally paths; fixture cleanup belongs to the existing disposable-project teardown.

Narrow corrections to existing interfaces: importer server errors now appear inside the open modal; academic-report filter changes abort/invalidate old generation/download state; academic-report PDFs use the existing identity/license-checked Georgia Bold loader for the full school name on every page. No new screen, permission, schema, migration, mark policy or backup version. Component regression tests exercise stale asynchronous responses and modal error placement. Actual local PDF service tests verify header/font/content and reject exchanged colour/monochrome modes without publishing PDFs or fonts.

Focused new evidence: 32 tests in four files PASS, no skips, including an isolated SQLite service journey prepared from every unchanged active migration. The harness stubs admission/MFA preparation and is explicitly not runtime or real-login proof. Failures cover wrong source/actor/Student/state/version, missing/duplicate effects, wrong denial/privacy, altered workbook/context, wrong report scope/audit mode, failed download, missing control and cleanup failure. Existing related academic-report/security tests also passed (13 tests). Sole read-only reviewer traced screens/selectors, role/scope, templates, separation, caller/readback, exports/privacy and classification; final disposition IMPLEMENTED_REVIEWED with no known in-scope source gap. Unchanged prior producer/H1–H3/CB/FB/16-fresh+16-repeat evidence is reused, not reclassified as new rendered evidence.

The genuine preview-expiry scenario consumes one existing 15-minute lifetime. Only the integrated acceptance subprocess receives the previous 20-minute action allowance plus those 15 minutes; other subprocess bounds, the producer's 40-minute outer bound and finally cleanup remain. No security clock or production expiry is changed. Actual hosted performance within those bounds is unproved and timeout remains a failure.

Execution prerequisites: qualified immutable production and distinct QA artifacts, signed same-run/replica capability, private hosted services and verified CA, pinned Playwright/Chromium/CDP and licensed Georgia Bold. No external PDF decoder is needed by this marks slice (bounded pdf-lib/fontkit inspects bytes); retained certificate decoder prerequisites remain separate. EXTERNAL_RUNTIME_BLOCKED and EPHEMERAL_EXACT_HEAD_CI_ONLY are unchanged. Actual rendering/login/HTTP/download/CDP/teardown NOT_EXECUTED. The broader integrated report stays PARTIAL and deliberately refuses completion; onboarding/native/historical and other required work is not claimed complete.

All 25 flags OFF/0%, 46 IDs/history, v48 and migration checksums, original PRs and OCR exclusion remain. Protected operational file was not accessed; prior fingerprint is reused only, and the denied integrity command was not retried/split. No real data, provider, package/image/private-template/session/font publication, historical deletion, merge/tag/deployment or activation. Final source/CI/security and tracker readbacks are in `tmp/marks-browser-acceptance-1a/final-report.md`; mandatory skipped jobs are not PASS.

## 2026-09-24 — RECOVERY-WINDOWS-AUTH-LIFECYCLE-1A bounded checkpoint

Starting source `621f45729f1bb492b8b397e19aa9d1be8f57998a`, tree `aea68a330fdbdaba3bf494f95c7497898d38c42a`. This updates only CROSS-PLATFORM-APPS-1A Windows authentication acceptance; no requirement ID or whole-product completion changes. Disposition: **WINDOWS_AUTH_LIFECYCLE_PARTIAL_WITH_EXACT_CODE_GAPS**. The prior local report-write/integrity refusals are respected; this existing ledger and PR28 carry the new handoff, not a recreated refused report file.

| ID | Executable implementation and meaningful check | Implementation | Execution / remaining prerequisite |
|---|---|---|---|
| WD1 | `windows-auth-lifecycle.ts` validates source/run/attempt, identity, architecture, actual known-folder layout and separate Edge/WebView driver versions. `windows-webdriver-host.ts`/`windows-host.ps1` implement loopback owned-listener checks, W3C session creation, exact process identity/descendants, pre-existing-profile refusal, salt continuity and bounded owned-process cleanup. | PARTIAL | UNIT_OR_DRIVER_CONTRACT. No admitted Windows native-artifact provenance/host factory or backend topology connection yet. Actual Windows launch NOT_EXECUTED. |
| WD2 | Rendered PIN establishment, wrong PIN after initial creation, cancellation before confirmation, original browser URL observation (exact allowed origin/path/parameters), real login/MFA helper, pending-device approval followed by a fresh request. Existing single-instance deep-link forwarding is enabled. | PARTIAL | UNIT_OR_DRIVER_CONTRACT. Real serving-target fixture/device governance and original OS-callback collector remain unwired; MFA/PKCE UI is not claimed executed. |
| WD3 | Exact source/database/user/device/request/session/reference assertions reject foreign targets, duplicate sessions, missing MFA and wrong reference population. App reference controls and original browser request are real W3C actions in the orchestration. | PARTIAL | UNIT_OR_DRIVER_CONTRACT. A signed stdin-only native probe/fixture adapter against the actual serving container is MISSING CODE; no independent live readback is claimed. |
| WD4 | Explicit lock, OS minimize/restore, same-profile process restart, incorrect PIN and subsequent refresh; token-version/rotation-history assertions and lock-screen MutationObserver checks. App masks immediately before persistence, rejects unlock across failed lock, cancels stale listeners/callbacks, rotates expired access and serializes refresh. | PARTIAL | UNIT_OR_DRIVER_CONTRACT plus Rust compilation/security unit tests. Observer is installed after W3C session creation: startup/return-before-lock-frame observation remains a coverage gap, not a proven no-flash result. |
| WD5 | Exact-session revocation orchestration, protected refresh refusal, restart/PIN cannot restore server authority, subsequent fresh request/session must differ and old session remain revoked. Denial whitelist excludes 500, timeout, flag-off and unrelated permission errors. | PARTIAL | UNIT_OR_DRIVER_CONTRACT. Exact session-governance/readback and callback adapter are MISSING CODE; no runtime revocation result. Device-only revocation is distinct and is not falsely treated as a revoked session row. |

Entrypoints are exported `runWindowsAuthentication(host, fixture)` and the concrete `OwnedWindowsWebDriver` platform adapter, reusing `NativeWebDriver`/`authenticateNativeBrowser`. There is deliberately no new unguarded CLI: the existing integrated caller lacks the production Windows host construction and remains incomplete. `OwnedWindowsWebDriver.start()` retains the existing immutable synthetic-artifact admission, whose current Linux-only boundary refuses Windows. A supplied Boolean or contract-test host cannot confer real admission; the orchestration result is labelled contract evidence and cannot qualify the aggregate native gate.

Remaining engineering, explicitly not vendor/device-only blockers:
1. Connect the existing integrated caller to a real Windows host factory and source/run-bound native package/profile/tool provenance admission. Existing NSIS compilation/checksums are not that admission.
2. Implement the signed same-serving-container native fixture/readback, device approval, session revocation and exact audit bindings; the current typed host ports are not implementations of those backend actions.
3. Implement the genuine OS callback collector bound to the launched instance and original request. Merely invoking `observeCallback` on a test double is not proof.
4. Finish startup/background privacy observation before the first potentially private frame, and validate actual WebView/user-data/process paths on the admitted disposable machine.
5. Reconcile the native Rust client's private-CA/profile boundary and the existing synthetic feature capability path. The current capability does not admit the native/offline flags, and Rust's WebPKI trust does not inherit Windows/Edge private-CA trust. No capability, CA, origin allowlist, trust store or production flag was broadened in this checkpoint.

External inputs also required: an authorised disposable Windows user/profile with the exact existing protocol handler/default-browser setup, pinned tauri-driver and distinct correctly matched WebView2/Edge driver binaries, a privately retained exact-source native artifact, and one admitted private backend actually reachable with valid TLS by the app, WebView and system browser. The current Linux job's loopback is not such topology. No cross-host tunnel, installed-owner-profile mutation, public package fallback, unqualified runtime launch or TLS bypass was attempted.

Focused validation: 77 root tests, 14 shared-client tests and 7 Rust unit tests PASS with zero skips; offline locked Cargo check, all 20 root TypeScript partitions, native TypeScript, Vite production web build and PowerShell syntax parsing PASS. Four complete npm audit views (root/native/bridge/migrator, including development dependencies) report zero findings; Git safety and public-source scan PASS. The public scanner initially refused the five newly authorised native source paths as out-of-scope; only those exact files were admitted and `.ps1` content scanning added, preserving all binary/secret/contact checks. The sole read-only reviewer traced app/helper/host changes, found and verified corrections for callback/rotation/lock races, listener/process ownership, original-tab selection and cleanup, and approved a retained PARTIAL checkpoint; not code completion. Local Rust emitted existing libsodium debug-symbol/default-library linker warnings but all seven tests passed. Full source/database/build/security and normal platform compilation jobs are delegated to the single consolidated exact-head CI push, with their final states recorded in PR28; no manual duplicate dispatch.

New tests exercise complete simulated orchestration ordering and failure cleanup, malformed/foreign URLs and session creation, wrong actor/source/profile/versions/references, duplicate authority, false revocation, foreign PID/listener and cleanup refusal; auth tests cover delayed registration, duplicate callbacks, abort during exchange and concurrent refresh rotation. Process/Page doubles prove control flow only. No WINDOWS_APP_EXECUTED, AUTHENTICATED_WINDOWS_BACKEND or OWNER_DEVICE_CERTIFICATION result exists. Existing server/native encryption evidence and closed producer/HTTP/CB/FB/MB/restore work are reused, not rerun as Windows authentication proof. Final exact-head CI and tracker readbacks are recorded in PR28 after the single source push.

Official tooling references reviewed: [Tauri external WebDriver](https://v2.tauri.app/develop/tests/webdriver/), [Microsoft WebView2 WebDriver version matching](https://learn.microsoft.com/en-us/microsoft-edge/webview2/how-to/webdriver), and the installed `tauri-plugin-single-instance` 2.4.3 source's `deep-link` feature forwarding. No new test framework/plugin or downloaded binary was introduced. Cleanup cannot promise recovery after uncatchable termination; unidentified descendants are reported as residue and left for authorised disposable-runner reconciliation, never killed by image name. No test processes/profiles were launched by this task's contract tests.

All operational flags, v48/schema/migrations, 46 IDs/history, original PRs, OCR exclusion, historical artifacts and denied residue are preserved. The operational database was not accessed; its prior fingerprint is reused only. Android/iOS, offline posting, file transfers, historical production, other Browser gaps, runtime qualification and real-device certification remain outstanding. No merge/tag/deployment/provider or staff activation.

The first normal CI push at `546a347` exposed two additional publication checks: communication source scope omitted the same five reviewed native files, and onboarding rejected fixed token-like strings in the new mock test responses. A corrective source checkpoint admits only those five paths to the communication scanner (including full Cargo.lock content scanning) and generates opaque mock values only inside the test process. No scanner secret/binary/contact rule was relaxed. Communication, real-user, onboarding and biometric publication scans now PASS locally; 24 communication and 14 shared-client tests PASS without skips. The sole reviewer approved both narrow corrections. Historical failed runs are retained; the necessary source correction receives normal final-head CI, not a duplicate dispatch or documentation-only push.

## 2026-09-24 — RECOVERY-WINDOWS-SERVER-PROBE-1A

Starting source `42fd1bfce9ee08a6f79ed50bbe4f7a0a271d31c0`, tree `c71abbc61bac769ed76ec58e1975ebdd2cdc06e3`. **WINDOWS_SERVER_PROBE_PARTIAL_WITH_EXACT_CODE_GAPS**. This is a bounded CROSS-PLATFORM-APPS-1A server-port checkpoint, not completion of WD1–WD5. Historical missing-code descriptions above describe their original checkpoints; this delta supersedes only the implemented server portions.

| ID | Implementation and meaningful evidence | Disposition / execution |
|---|---|---|
| SP1 | `windows-server-contract.ts` strict allowlist, 4096-byte stdin, exact keys/types, source/run/attempt/phase/database/device/key/iteration binding; `windows-server-probe.ts` validates existing signed serving target before lazy client construction, rechecks before/after operations and disconnects in finally. Two synthetic actors, real MFA-enrolment helper, two eligible and one excluded Student; no session/callback/approval outcomes seeded. Reuse/foreign target refusal and failure disconnect tested. | IMPLEMENTED_REVIEWED / UNIT_OR_ADAPTER_CONTRACT and isolated SQLite fixture/service evidence. Signed live container NOT_EXECUTED. |
| SP2 | Actual request/device/web-session/role chain, original Ed25519 browser proof, explicit projections, exact correlated native session and real rotation history. `NATIVE_SESSION_CREATED` gains its actual public request ID in existing safe details, with no schema/policy change. Null pending user/session/version, activeSessions scoped to the correlated session, separate duplicate-authority refusal, old revoked session stays zero when a fresh session exists. | PARTIAL / ISOLATED_SERVICE. Session-linked MFA proof and observed reference-refresh proof remain missing; null MFA and AVAILABLE_POPULATION_ONLY_NOT_REFRESH_PROOF cannot satisfy WD3. |
| SP3 | Approval calls existing private TLS HTTP PATCH device route after real governance login/MFA, then verifies exact device approver and one real DEVICE_APPROVED event. Service tests invoke the actual route with simulated login/transport and real permission evaluation; missing permission and repeat approval fail. | PARTIAL. Existing route has no step-up consumption; none is invented. Exact-session administrator revocation remains MISSING CODE and explicitly refuses after ownership validation. |
| SP4 | `createAdmittedWindowsServerPorts` uses existing immutable QA admission, `inspectTarget` all-replica binding and bounded docker stdin pipe to bundled `browser-probe.mjs windows-native`; `runWindowsWithServerPorts` connects concrete methods to retained lifecycle. Original URL must be observed before read/approval; session must be read before revoke. Platform admission precedes fixture writes. Private in-memory inputs clear in finally. | PARTIAL / UNIT_OR_ADAPTER_CONTRACT. Linux controller invocation exists; an admitted Windows-to-controller transport/topology and complete platform host remain absent. This is not a running cross-host bridge. |

Only implementation paths changed: `scripts/portable/windows-server-contract.ts`, `windows-server-probe.ts`, `windows-server-adapter.ts`, `browser-probe.ts`, `windows-auth-lifecycle.ts`, and `lib/native-app/auth.ts`. Tests: `tests/windows-server-probe.test.ts`, `tests/windows-server-service.test.ts`. The existing browser-probe bundle entry includes the handler; no public fixture route, arbitrary SQL/Prisma/command/database selection or new signing framework exists.

The fixture requires an independently known public device ID/key hash from the future owned native profile, plus a fresh admitted serving target whose eligible reference population is empty. The current Accountant reference service includes all active Students, so this probe refuses a populated or mixed target instead of inventing row-level reference restrictions. The unique fixture username reserves its run/device; its ownership marker also binds iteration, key and database. Repeated or partially completed preparation refuses and leaves task-owned state for the existing disposable-runner teardown; it never overwrites another actor. Fixture provisioning is not enrolment/training/MFA-screen acceptance. Passwords, MFA responses and original URLs remain private stdin/in-memory data; no factor seed, bearer token, password hash, device private key or raw credential row is returned. Clearing memory is not forensic erasure.

Exact remaining engineering:
1. Existing `revokeNativeSession` is native bearer-authenticated self-logout. Existing administrator device revocation changes the entire device and prevents same-device reconnect; it is not exact-session revocation. No current administrator session-ID service/route or corresponding reviewed authority contract exists. This task does not invent a permission policy or directly set revoked columns. `revoke-session` therefore throws WINDOWS_EXACT_SESSION_GOVERNANCE_NOT_IMPLEMENTED after verifying the requested chain. A reviewed exact-session governance service/contract is needed before this port can complete.
2. MFA_LOGIN_SUCCEEDED identifies a challenge; AuthSession does not record that challenge relationship. This adapter reports NO_SESSION_CHALLENGE_LINK_RECORDED and mfaUsed=null instead of using an unrelated historical MFA count. The observed login challenge/session connection must be implemented before a native MFA acceptance claim.
3. Reference packs have signed snapshot versions but no persisted delivery/refresh record. This adapter reports the independently checked available synthetic population and a population digest, not delivered data. The future app observation must supply its actual snapshot version and verify the existing user/device signature contract; server availability alone cannot close that gap.

Focused validation at this checkpoint: 98 tests in eight files PASS, zero skips, including actual SQLite native request/approval/exchange/rotation/self-logout/fresh-authentication services using valid generated Ed25519/PKCE inputs. These are SERVICE-TEST inputs, not an observed Windows PKCE request or OS callback. Admission, HTTP login/transport and MFA preparation are explicit doubles in the isolated service harness. Approval audit is created by the route, session audit by exchange, and history by rotation. No expected approval/session/audit outcome is inserted by fixtures. Negative tests cover changed target, cross-device/key/iteration, altered proof, insufficient governance permission, repeated approval, foreign/unobserved session, unknown/oversized inputs, private output fields, duplicate active authority and cleanup failure. Self-logout evidence does not satisfy missing administrator revocation. PostgreSQL uses the existing admitted CI provider path with an owned unique schema; local PostgreSQL was NOT_EXECUTED. Final exact-head provider/regression/build/security status is recorded in PR28, not assumed here.

### 2026-09-24 — RECOVERY-NATIVE-SESSION-REVOCATION-1A delta

This dated delta supersedes only the missing exact-session operation in SP3 above. The original checkpoint and its other gaps remain historical evidence. Starting source `b08ef2d3670c65f37d5c5d736dfd279bb9bf1e21`, tree `9556b3c4a8cdb4597ba66de49e62c00a291d013b`; same owned branch/worktree and draft PR28. The iPad unchanged-source retry already passed (run35952178501 attempt2/job107529684236); no further retry or durable-fix claim is made here.

| Bounded gap | Implementation | Evidence / execution |
| --- | --- | --- |
| SP3 administrator exact native-session revocation | IMPLEMENTED_REVIEWED: `lib/native-app/session-governance.ts` and normal `POST /api/native-auth/sessions/[id]/revoke`. Actual web cookie is resolved inside a Serializable transaction; active selected SUPER_ADMIN, effective MANAGE_OFFLINE_SYNC_DEVICES including explicit deny, no pending password change, and existing single-use step-up bound to full target UUID/action/actor/session/environment are required on every request. Reason 4–160 characters, no controls; strict body keys. Native/offline gates and middleware rate/origin/body protection retained. | ISOLATED_SERVICE_OR_ROUTE: generated Ed25519/PKCE native request→authorization→exchange and real MFA/step-up. Unknown/corrupt relationship, actor/role/permission and step-up denials; rollback, retries, rotation, exact audit and control preservation. No public fixture endpoint or new schema. |
| Durable transition and credential rejection | IMPLEMENTED_REVIEWED: selected session CAS and NATIVE_SESSION_ADMIN_REVOKED audit commit together; original time/reason/actor remain. Repeat consumes a fresh authorized grant without a second transition. Refresh rotation and reuse now CAS against unrevoked state, preserving first revocation and rolling back losing history writes. | Deterministic sequential refresh-first and revoke-first tests plus nondeterministic overlapping refresh/revoke and concurrent revoke. Both current/rotated credentials rejected after commit; fresh login creates another session. Separate-device native control and web session preserved; device and user snapshots unchanged. This does not cancel requests authorized before commit or erase disconnected local data. |
| Private Windows consumer | IMPLEMENTED_REVIEWED: existing stdin dispatcher/target admission → `windows-server-probe.ts` → real governance login, realStepUp and normal HTTP action → exact session/audit readback → `windows-server-adapter.ts` retained server port. Grant consumption and first-event operation/session are independently checked; repeats compare unchanged original event. Other owner sessions' projected revocation/version state and device approval are checked. | UNIT_OR_ADAPTER_CONTRACT plus ISOLATED_SERVICE_OR_ROUTE. Probe service tests double admission/login/transport only; actual MFA, step-up, revocation route, permissions, audit and DB transitions run. ADMITTED_SERVING_CONTAINER and AUTHENTICATED_WINDOWS: NOT_EXECUTED. |

Changed implementation paths: the new service/route above, `lib/native-app/auth.ts`, transaction-client typing in `lib/real-user-access/step-up.ts`, and existing `scripts/portable/windows-server-{contract,probe,adapter}.ts`. Meaningful tests: `tests/native-session-governance.test.ts`, `tests/windows-server-{service,probe}.test.ts`. Existing caller/bundle is reused. One focused independent read-only reviewer traced the whole connection; its two findings (credential-safe test diagnostics and precise audit/grant correlation) were corrected and reviewed again with no remaining scoped blocker.

Local evidence: the initial 85 focused native/Windows/readiness tests across seven files passed with zero skips, including 18 affected tests across three files. Shared native-client tests also pass (14 tests). Full partitioned typecheck, portable bundle, Git-safety and public-source/privacy scans pass; root and workspace dependency audit reports zero findings across production and development dependencies; exact final-head CI, provider regression/build/security results and coordinates are recorded in PR28, never inferred from these local tests. PostgreSQL uses the existing admitted hosted synthetic schema path; no local PostgreSQL or application stack was launched.

Hosted run35974147867/job107550524824 on `c3c2e5659933596bc1669188addea4fdb40b632a` exposed the expanded probe journey exceeding its existing 15-second test budget (15002 ms). Its real MFA/step-up/service sequence is now five guarded sequential phases at authoritative state boundaries, each retaining the same 15-second cap and every original assertion. Failed predecessors cause explicit prerequisite failures, never skipped/pass successors. This is four additional phase results, not four new product scenarios. The affected selection is now 22 tests. A local fixture sign-in failure also prompted explicitly already-effective synthetic role start times; no production clock or authority policy changes. The sole read-only reviewer checked both corrections. The original CI failure remains retained; normal corrected-head CI is reported separately.

At `7ead211795bd4affd8a7329ce683237df34feb96`, PostgreSQL passed all new service/probe cases and its full regression/build (2754 passed, three inherited qpdf skips). Two Windows jobs nevertheless timed out in different grouped governance tests: run35976269526/job107557766003 (step-up variants; an unchanged finance service test also timed out) and run35976269509/job107557396756 (caller reauthorization). These are retained failures, not a proven transient-runner diagnosis. Real MFA/native-session fixture preparation now has a separate 60-second per-test hook budget; each actual action retains the existing 15-second limit. The nine existing step-up rejection cases now have independent targets and individual assertions, with no revocation/audit result seeded. All original cases remain and every denied case checks no transition event plus valid original authority. The affected selection passes 30 tests; the eight extra results expose existing variants rather than new scope. Production code and action timeouts/assertions are unchanged. GitHub refused a failed-job retry while the native workflow was active; no retry began. Further normal new-head CI, not an unchanged retry loop, validates this correction.

The mandatory source-provenance check initially found the omitted registration of the new route. The existing generator updated `config/recovery-integration-source-delta.json` for exactly seven reviewed implementation paths (three new entries, four current hashes). Independent review verified actual hashes and unchanged historical coordinates; the master/source, publication and authentication-backup selection then passed 40 tests. The retained SQLite recovery/publication/authentication-backup selection passed 29 tests; its sanitized matrix reports eight fresh plus eight repeat restores across v45/v46/v47/v48. PostgreSQL supplies the other eight fresh/eight repeat cases through final-head CI. The private recovery fixtures remain in ignored run-owned storage; no denied-residue cleanup was attempted.

No known code gap remains in this exact-session service/request/probe connection. SP2/WD3 still report `NO_SESSION_CHALLENGE_LINK_RECORDED` and `AVAILABLE_POPULATION_ONLY_NOT_REFRESH_PROOF`; no invented MFA or refresh proof. Full Windows host/artifact admission, original OS callback collection, earliest-frame privacy, private CA/profile/native-QA connection, other platform/offline/file/historical work and EXTERNAL_RUNTIME_BLOCKED remain. Overall integrated/native verdict remains PARTIAL. No schema/migration/v48/flag/requirement or original PR changes. Operational DB untouched; prior fingerprint is reused historical evidence, not freshly verified. Fresh native-session/probe test databases were removed by their ownership-checked teardown; historical artifacts and denied residue preserved.

The sole read-only reviewer traced dispatcher → admission/ownership → real service/route → projection → server ports → retained lifecycle. Review found and verified corrections for eager singleton construction, duplicate authority hidden by a scoped count, and preparation preceding platform admission. Final acceptance is a retained PARTIAL checkpoint, not code completion. Portable bundle, root dependency audit (production/development zero findings), Git safety and four publication scans passed locally; all twenty TypeScript partitions and normal required CI are checked on the coherent candidate with final results in PR28. No workflow was weakened or manually duplicated.

ADMITTED_SERVING_CONTAINER and AUTHENTICATED_WINDOWS remain NOT_EXECUTED. EXTERNAL_RUNTIME_BLOCKED, immutable image/replica/TLS admission and EPHEMERAL_EXACT_HEAD_CI_ONLY remain unchanged. Native/offline flags are still absent from synthetic feature admission; no flag override was added. Existing native artifact provenance/full host factory, genuine instance-bound callback collector, earliest-frame privacy, Rust/private-CA/profile connection and real topology remain separate missing work. Android/iOS, offline posting/transfers, historical images and all other packages are untouched.

All 25 operational flags OFF/0%, v48/historical contracts, schemas/migrations, 46 requirement IDs/history, original PRs and OCR exclusion remain. Operational DB was not accessed; its old fingerprint is historical evidence only. Refused integrity/report commands, historical artifacts and denied residue were not retried/deleted. Only newly created test-owned temporary databases are removed after inode/device/path ownership checks and client disconnection. No native processes, school profiles, real data, provider, package/image publication, merge/tag/deployment or activation. Tracker deltas/readbacks and elapsed/account usage, if observed, are recorded in PR28/final handoff. No separate refused report file is recreated.

First source push `ccc636517e21479aae709a23bdc21ff5bde1bc8c` (tree `773a9cb69a03377dfcfee7f5ec137988384c71b1`) failed the mandatory master provenance gate: the current integration register omitted the newly reviewed `lib/native-app/auth.ts` request-correlation change. The corrective checkpoint updates `config/recovery-integration-source-delta.json` for exactly the six reviewed implementation paths above, preserving every baseline/source-head hash and historical register. This records understood source changes, not a relaxed assertion or historical evidence replacement. The unchanged gate and all 16 master-reconciliation tests now pass locally. Original failed CI is retained; only the necessary provenance correction receives normal new-head CI. No full-workflow manual rerun or unrelated cancellation.
