# NATIVE-PUSH-CERTIFICATION-1B — DO NOT RUN

Run only after explicit private-staging, device, credential and distribution approval. Require approved FCM/APNs projects and credentials in secret storage; separate staging/production environments; actual supported Android and iOS devices; current app builds; registered user/device ownership; token rotation/revocation; and a documented support/rollback owner.

Test permission education and denial/re-prompt behavior without requesting on launch; foreground/background/terminated delivery; generic lock-screen privacy; notification grouping; expiry; deep-link authentication and current object authorisation; wrong-user/token substitution; logout/offboarding/device revoke; app/version/environment mismatch; duplicate/reordered receipt where available; offline recovery; rate/cost limits; accessibility; battery/network interruption; Android OEM and iOS focus-mode behavior. Simulator evidence is supplementary only.

Keep production and every unrelated communication flag OFF. Do not include marks, finance detail, medical/CWSN, biometric, complaint, authentication secrets, OCR content or private attachments in push payloads. Do not claim physical-device certification without captured device evidence.
