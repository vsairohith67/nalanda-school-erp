# PORTABLE-DEPLOYMENT-OBSERVABILITY-FOUNDATION-1B

**GENERATED_NOT_EXECUTED. This file is an independently runnable future task description, not an instruction to execute during MASTER-REQUIREMENTS-RECONCILIATION-1A. It requires a later explicit owner start.**

Work on `vsairohith67/nalanda-school-erp`. First fetch origin, inspect clean status/branches/worktrees/stashes/open PRs, and resolve the annotated `master-requirements-reconciliation-v45-2026-09-04` tag (or the exact suffix recorded in the completed reconciliation release). Verify its normal two-parent merge, tree, retained feature head, exact-head terminal CI and clearance evidence. Do not assume the reconciliation starting main `4a4df050d194104cfc497a6de790ca9553a69db6` is this task's base. If the release is absent, unclear, failed or main has advanced beyond the owner-authorised base, stop implementation and produce a read-only delta/entry-gate report. Require current repository visibility and explicit source-only authority. Preserve OCR PR #19 open/unmerged/unchanged at `1ba360b123ded770f1554d59fbd21c86b9943427`; if it differs, stop and report.

Create one dedicated retained worktree/branch for this phase only, after the base is admitted. Record the actual operational database path and hash/size/mtime/sidecars without querying its records. All runtime QA uses fresh synthetic databases or their copies. Keep original database bytes and sidecars unchanged. Read the living register, target matrix, observability decision and Product Experience debt before changing code. Update NPS-REQ-040 and relevant NPS-REQ-041/046 evidence in a new register version with priorRegisterHash; do not relabel unrelated missing requirements.

## Authorised implementation scope

1. Consolidate the existing root Dockerfile/portable bundle into **one canonical OCI application image**, one versioned environment/secrets contract and small profiles. Reuse current web/migration/job dispatch; do not create provider-specific application forks.
2. Implement `local-single-node` and `generic-vps` profile definitions and no-deploy plan/validation tooling. Describe managed-cloud capability interfaces only, without provisioning accounts, resources or providers. Keep local development separate; document local-resilient admission and defer Kubernetes unless separately justified.
3. Implement safe operator commands for preflight, doctor, install/initialise, migrate, backup, restore, upgrade, rollback and non-destructive uninstall, with dry-run defaults. Deliver executable command behavior tested through synthetic filesystem/process adapters, including partial failures and resume; a plan-only document does not satisfy implementation. Reuse migration locks, encrypted backups and current schema-compatible rollback. Require explicit target paths and empty-target/provenance/compatibility checks; preserve data/volumes/keys by default. No implicit purge or destructive down migrations. Executing these commands against an ERP deployment remains prohibited during this phase.
4. Validate secret-file/env inputs, permissions, required endpoints, safe errors, CPU architecture, resources, ports, storage and readiness. Produce x86_64 and arm64 build/provenance/SBOM/scanner evidence using permitted build/test infrastructure; never claim an emulated build certifies physical hardware. Builds are permitted; installing/running the ERP deployment stack is not. Stop with an explicit gate if required multi-architecture evidence is unavailable.
5. Add the OpenTelemetry-first **no-network synthetic foundation**: strict event schema, enum-valued safe codes/routes, bounded sinks, deterministic clocks/IDs where needed for tests, queue/sampling/resource controls and default-disabled exporters. Minimise at source; do not export existing arbitrary structured-log strings without validation. No raw request/body/header/cookie/query/URL/SQL/record/financial/document/support/OCR content, identity-derived correlation or session replay.
6. Extend Technical Operations with safe telemetry/provider states while retaining its authority for school health/incidents/maintenance. Core operations must return identical results with optional telemetry absent, disabled or failed. Provider failure must never block school transactions or trigger transaction retries.
7. Define optional Sentry/PostHog adapter interfaces behind default-off flags and disabled sinks. No account, DSN, API key, telemetry account, external endpoint or network call is authorised. Keep replay/autocapture OFF. Existing release flags remain the authority; PostHog cannot grant school permissions or turn on product capabilities.
8. Maintain the current v45 logical backup unless an unavoidable separately reviewed data-contract change is proved. Prefer source configuration over persistence. Add migration only if essential and explicitly justified by the bounded requirement; never rewrite existing migrations.

## Verification and independent review

Prove schema/redaction with adversarial synthetic fixtures, unknown/nested/encoded fields, private values in safe-looking fields, path/query normalisation, correlation boundaries and cardinality caps. Prove zero network in provider-disabled/local synthetic modes and timeout/DNS/credential/queue/full-disk failure isolation. Test every operator partial-failure/resume and rollback step through synthetic adapters; no real install or deployment.

Run focused tests first, public-source/real-contact/secret scans, dependency/license audit, Prisma validate/generate, SQLite fresh/copy/repeat migrations, PostgreSQL 17 parity, encrypted backup/restore twice, affected security/resilience and portability/Technical Operations suites, cross-platform contracts, full Vitest, all TypeScript partitions and production build. Where a no-deployment restriction prevents a runtime check, record an explicit gate rather than passing it. Browser QA may run only a separately isolated synthetic test harness for affected UI under the owner's later task authorization; never expose real users/data or deploy the ERP. Cover desktop/mobile, light/dark, keyboard/reduced-motion, permitted/denied roles, loading/error/blocked/degraded states, overflow and console/hydration. Apply Georgia Bold branding to generated documents/images.

Use independent platform, application security, identity, privacy, school administration, finance, HR/child safety, accessibility and release reviews. Correct zero-tolerance findings; require zero unresolved Critical/High or authorization/privacy/financial-integrity/document-authenticity/child-safety Medium. Seal an immutable final changed-file security scan after corrections. Recheck operational database identity, all feature flags and OCR PR coordinates.

## Release and stop boundaries

Push exact source head, create a normal PR, wait for terminal exact-head CI and verify all mandatory gates before a normal two-parent merge. Retain branch/history; no squash/rebase/force. Create an annotated phase-specific tag only after merge and verify exact peeling. Reconcile existing canonical tracker records once after terminal Git evidence and read each update back; produce a deterministic unapplied patch where actions are unavailable. Do not use a clearance marker on skipped/failed mandatory gates.

No real data, user activation, invitation, message, payment, OCR/model execution or weights; no provider selection/configuration, resource purchase/provisioning, DNS, secrets, deployment or operational activation. Do not execute any other bundle. End with exact Git/CI/security/test/database evidence, remaining gates, registry/changelog changes, tracker readbacks and an explicit no-activation statement. The present file was generated during reconciliation and has not been executed there.
