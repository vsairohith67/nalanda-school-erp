# PRIVATE-STAGING-1B-PROVIDER-ADAPTER

Planning and owner-approval gate. Do not create resources until every required value is explicit.

Consume exact `PORTABLE-STAGING-FOUNDATION-1A`. Record owner-approved provider; account/project/tenant; region/residency; budget/alerts; compute; PostgreSQL 17 and identities; authenticated TLS Valkey; private S3-compatible storage; secret manager/mount; edge/WAF/private access; DNS/TLS; database and object backup/replication; monitoring/log retention; support/incident owner; DPA/legal approval.

Prove private image visibility/digest, segmentation, no data ports, no web `DIRECT_URL`, migration lock, two replicas, health/outages, encrypted backup, repeated restore, object recovery, upgrade/rollback, native compatibility, zero real data/users and flags off. Stop on any missing approval, budget, region, access, backup, DNS/TLS authority, contract mismatch or security finding. Do not select a provider in this template.
