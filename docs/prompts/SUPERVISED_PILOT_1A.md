# SUPERVISED-PILOT-1A — Future Prompt (Do Not Run)

Start only after every category in `PILOT_GO_NO_GO_GATES.md` has a named owner and explicit GO. Onboard a small named cohort, deliver role training, run opening/attendance/fee/closing/report/support workflows under supervision, monitor security/health/reconciliation, retain legacy fallback and execute rollback on defined triggers. Record issues without widening roles or activating optional providers. A synthetic pass, private staging deployment or green CI alone cannot authorize real users or production cutover.
