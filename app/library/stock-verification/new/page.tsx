import Link from "next/link"; import { StockSessionCreateForm } from "@/components/library-stock-verification-forms"; import { LibraryNav } from "@/components/library-nav"; import { PageHeader, PageShell } from "@/components/ui"; import { requirePermission } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { getSchoolSettings } from "@/lib/school-settings";
export default async function NewStockVerificationPage(){await requirePermission("MANAGE_LIBRARY_STOCK_VERIFICATION");const [titles,settings]=await Promise.all([prisma.libraryTitle.findMany({select:{id:true,titleCode:true,title:true},orderBy:{titleCode:"asc"}}),getSchoolSettings(prisma)]);return <PageShell className="library-page"><PageHeader title="Create Library Stock-Verification Session" description="Preview first. Starting later freezes one expected-copy snapshot; scanning never alters LibraryCopy." action={<Link className="button secondary" href="/library/stock-verification">Back to sessions</Link>}/><LibraryNav current="stock-verification"/><StockSessionCreateForm titles={titles} academicYear={settings.academicYear}/></PageShell>}
