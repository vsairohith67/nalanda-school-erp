import{PageHeader}from"@/components/ui";import{CertificateRequestForm}from"@/components/certificate-forms";import{requirePermission}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{getSchoolSettings}from"@/lib/school-settings";
export default async function NewRequestPage(){await requirePermission("MANAGE_CERTIFICATE_REQUESTS");const[students,settings]=await Promise.all([prisma.student.findMany({where:{deletedAt:null},select:{id:true,studentName:true,admissionNo:true},orderBy:{studentName:"asc"}}),getSchoolSettings(prisma)]);return <div className="page certificate-page"><PageHeader title="New Internal Certificate Request" description="Purpose and copies are validated. No authoritative Student facts can be edited here."/><CertificateRequestForm students={students} academicYear={settings.academicYear}/></div>}
