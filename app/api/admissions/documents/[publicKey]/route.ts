import { NextRequest, NextResponse } from "next/server";
import { admissionsError, ADMISSIONS_PRIVATE_HEADERS, requireAdmissionsAny } from "@/lib/admissions-api";
import { retrieveApplicationDocument } from "@/lib/admissions-files";
import { prisma } from "@/lib/prisma";
export async function GET(_: NextRequest, context: { params: Promise<{ publicKey: string }> }) { const auth = await requireAdmissionsAny(["MANAGE_ADMISSION_DOCUMENTS", "REVIEW_ADMISSION_APPLICATIONS"]); if (auth.response || !auth.user) return auth.response; try { const row = await retrieveApplicationDocument(prisma, (await context.params).publicKey, { actor: auth.user }); return new NextResponse(new Uint8Array(row.bytes), { headers: { ...ADMISSIONS_PRIVATE_HEADERS, "Content-Type": row.document.mediaType, "Content-Length": String(row.document.byteSize), "Content-Disposition": `attachment; filename="${row.document.safeDisplayName.replace(/["\\\r\n]/g, "")}"`, "X-Document-SHA256": row.document.sha256 } }); } catch (error) { return admissionsError(error); } }
