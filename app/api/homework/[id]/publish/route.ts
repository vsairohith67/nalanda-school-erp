import { NextRequest, NextResponse } from "next/server";
import { requireApiPermission } from "@/lib/auth"; import { prisma } from "@/lib/prisma";
import { parseExpectedUpdatedAt, publishHomework, serializeHomework } from "@/lib/homework"; import { homeworkError, loadAccessibleHomework } from "@/lib/homework-api";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireApiPermission("PUBLISH_HOMEWORK");if(auth.response)return auth.response;try{const id=decodeURIComponent((await params).id);await loadAccessibleHomework(auth.user,id);const body=await request.json();const row=await publishHomework(prisma,id,parseExpectedUpdatedAt(body.expectedUpdatedAt),auth.user.id);return NextResponse.json({assignment:serializeHomework(row,{includeInternal:true})});}catch(error){const result=homeworkError(error);return NextResponse.json({error:result.error},{status:result.status});}}
