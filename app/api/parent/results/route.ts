import { safeClientError } from "@/lib/client-errors";
import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { getParentReportCards, ReportCardPortalAccessError } from "@/lib/report-card-portals";
export async function GET(request: NextRequest) { const auth = await requireApiPermission("VIEW_OWN_REPORT_CARDS"); if (auth.response) return auth.response; try { return NextResponse.json(await getParentReportCards(prisma, auth.user.id, request.nextUrl.searchParams.get("student"))); } catch (error) { const status = error instanceof ReportCardPortalAccessError ? error.status : 400; return NextResponse.json({ error: safeClientError(error, "Unable to load report cards.") }, { status }); } }
