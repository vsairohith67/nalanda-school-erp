import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { getTeacherReportCards } from "@/lib/report-card-portals";
export async function GET(request: NextRequest) { const auth = await requireApiPermission("VIEW_REPORT_CARDS"); if (auth.response) return auth.response; if (auth.user.role !== "TEACHER") return NextResponse.json({ error: "Teacher account required." }, { status: 403 }); return NextResponse.json(await getTeacherReportCards(prisma, auth.user as any, request.nextUrl.searchParams.get("academicYear") ?? undefined)); }
