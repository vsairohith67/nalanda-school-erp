import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { getTeacherHomeworkData } from "@/lib/homework-portals"; import { prisma } from "@/lib/prisma";
export async function GET(request:NextRequest){const auth=await requireApiPermission("VIEW_OWN_HOMEWORK_PORTAL");if(auth.response)return auth.response;if(auth.user.role!=="TEACHER")return NextResponse.json({error:"Teacher homework is available only for Teacher accounts."},{status:403});return NextResponse.json(await getTeacherHomeworkData(prisma,auth.user as never,request.nextUrl.searchParams.get("academicYear")??undefined));}
