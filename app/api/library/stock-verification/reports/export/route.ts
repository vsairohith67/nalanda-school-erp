import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { libraryStockCsv, loadLibraryStockReports, stockReportFilename } from "@/lib/library-stock-reports"; import { prisma } from "@/lib/prisma";
export async function GET(request:NextRequest){const auth=await requireApiPermission("EXPORT_LIBRARY_STOCK_REPORTS");if(auth.response)return auth.response;const report=await loadLibraryStockReports(prisma,{academicYear:request.nextUrl.searchParams.get("academicYear")??undefined,status:request.nextUrl.searchParams.get("status")??undefined});return new NextResponse(libraryStockCsv(report),{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":`attachment; filename="${stockReportFilename()}"`,"cache-control":"no-store"}});}
