import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { loadLibraryStockReports } from "@/lib/library-stock-reports"; import { prisma } from "@/lib/prisma";
export async function GET(request:NextRequest){const auth=await requireApiPermission("VIEW_LIBRARY_STOCK_REPORTS");if(auth.response)return auth.response;return NextResponse.json(await loadLibraryStockReports(prisma,{academicYear:request.nextUrl.searchParams.get("academicYear")??undefined,status:request.nextUrl.searchParams.get("status")??undefined},auth.user.role==="VIEWER"));}
