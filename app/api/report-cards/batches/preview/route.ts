import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { previewBatchStudents } from "@/lib/report-cards"; import { reportCardApiError } from "@/lib/report-card-api";
export async function POST(request: NextRequest) { const auth = await requireApiPermission("MANAGE_REPORT_CARD_BATCHES"); if (auth.response) return auth.response; try { const preview = await previewBatchStudents(prisma, await request.json()); return NextResponse.json({ preview: { students: preview.students, reportType: preview.reportType, examCode: preview.examCycle?.examCode ?? null } }); } catch (error) { return reportCardApiError(error); } }
