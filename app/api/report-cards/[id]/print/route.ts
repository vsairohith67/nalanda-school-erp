import { NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { loadScopedReportCard, reportCardApiError } from "@/lib/report-card-api";
export async function GET(_: Request, { params }: { params: Promise<{ id: string }> }) { const auth = await requireApiPermission("VIEW_REPORT_CARDS"); if (auth.response) return auth.response; try { const card = await loadScopedReportCard(auth.user, (await params).id); const version = card.versions.find((item) => item.versionNumber === card.currentVersionNumber); return NextResponse.json({ payload: version ? JSON.parse(version.snapshotJson) : { status: "DRAFT PREVIEW", reportType: card.reportType, reportCardNumber: card.reportCardNumber, academicYear: card.academicYear, student: { name: card.student.studentName, admissionNumber: card.student.admissionNo, rollNumber: card.student.rollNo, className: card.className, section: card.section }, data: JSON.parse(card.draftDataJson), comments: { teacher: card.teacherOverallComment, principal: card.principalComment, director: card.directorComment }, promotionDisplayText: card.promotionDisplayText } }); } catch (error) { return reportCardApiError(error); } }
