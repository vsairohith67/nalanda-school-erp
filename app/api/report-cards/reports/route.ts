import { NextRequest, NextResponse } from "next/server"; import { requireApiPermission } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { buildReportCardReport, maskReportCardReportForViewer } from "@/lib/report-card-reports";
export async function GET(request: NextRequest) { const auth = await requireApiPermission("VIEW_REPORT_CARD_REPORTS"); if (auth.response) return auth.response; const q = request.nextUrl.searchParams; const report=await buildReportCardReport(prisma, { academicYear: q.get("academicYear") ?? undefined, className: q.get("className") ?? undefined, section: q.get("section") ?? undefined, status: q.get("status") ?? undefined }); return NextResponse.json(auth.user.role==="VIEWER"?maskReportCardReportForViewer(report):report); }
