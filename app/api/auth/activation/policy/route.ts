import { NextRequest } from "next/server"; import { prisma } from "@/lib/prisma"; import { activationEnvironment,activationFeatureUnavailable,activationJson,activationToken,readBoundedAccessJson } from "@/lib/real-user-access/activation-api"; import { acknowledgeActivationPolicy } from "@/lib/real-user-access/workflow"; import { isOperationalReleaseFeatureEnabled,REAL_USER_ACCESS_READINESS_FEATURE } from "@/lib/release-feature-flag-runtime";
export async function POST(request:NextRequest){if(!isOperationalReleaseFeatureEnabled(REAL_USER_ACCESS_READINESS_FEATURE))return activationFeatureUnavailable();try{const body=await readBoundedAccessJson(request);await acknowledgeActivationPolicy(prisma,{activationToken:activationToken(request),environment:activationEnvironment(),acknowledgement:String(body.acknowledgement??"")});return activationJson({success:true});}catch{return activationJson({error:"Policy acknowledgement was refused."},400);}}
