import { NextRequest } from "next/server"; import { prisma } from "@/lib/prisma"; import { activationEnvironment,activationFeatureUnavailable,activationJson,activationToken,clearActivationCookie } from "@/lib/real-user-access/activation-api"; import { completeAccountActivation } from "@/lib/real-user-access/workflow"; import { isOperationalReleaseFeatureEnabled,REAL_USER_ACCESS_READINESS_FEATURE } from "@/lib/release-feature-flag-runtime";
export async function POST(request:NextRequest){if(!isOperationalReleaseFeatureEnabled(REAL_USER_ACCESS_READINESS_FEATURE))return activationFeatureUnavailable();try{const result=await completeAccountActivation(prisma,{activationToken:activationToken(request),environment:activationEnvironment()});const response=activationJson(result);clearActivationCookie(response);return response;}catch{return activationJson({error:"Activation requirements are incomplete."},400);}}
