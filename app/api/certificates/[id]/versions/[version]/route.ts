import{NextRequest,NextResponse}from"next/server";import{requireApiPermission}from"@/lib/auth";import{parsePositiveIntegerPathParameter}from"@/lib/path-parameters";import{prisma}from"@/lib/prisma";import{parseCertificateSnapshot}from"@/lib/student-certificates";
export async function GET(_:NextRequest,{params}:{params:Promise<{id:string;version:string}>}){const auth=await requireApiPermission("VIEW_CERTIFICATES");if(auth.response)return auth.response;const p=await params,v=parsePositiveIntegerPathParameter(p.version);if(v===null)return NextResponse.json({error:"Version must be a positive integer."},{status:400});const row=await prisma.studentCertificateVersion.findUnique({where:{certificateId_versionNumber:{certificateId:p.id,versionNumber:v}}});if(!row)return NextResponse.json({error:"Version not found."},{status:404});return NextResponse.json({version:{versionNumber:row.versionNumber,versionType:row.versionType,certificateNumber:row.certificateNumber,issuedAt:row.issuedAt,snapshot:parseCertificateSnapshot(row.snapshotJson)}});}
