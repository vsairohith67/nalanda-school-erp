"use client";
import { useEffect, useState } from "react";
export function PayrollReports({ canExport }: {canExport:boolean}) { const [data,setData]=useState<any>(null),[error,setError]=useState("");useEffect(()=>{fetch("/api/payroll/reports",{cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error);setData(b);}).catch(e=>setError(e.message||"Reports could not be loaded."));},[]);if(error)return <div className="form-error" role="alert">{error}</div>;if(!data)return <p>Loading governed payroll reports…</p>;return <div className="payroll-workspace"><div className="section-heading"><div><h2>Approved and locked payroll only</h2><p>No Staff salary ranking exists. Small department groups are suppressed.</p></div>{canExport&&<form method="post" action="/api/payroll/reports"><button className="button primary">Export formula-safe CSV</button></form>}</div><div className="payroll-card-grid"><article className="card"><h3>Issued payslips</h3><strong className="metric">{data.issuedPayslipCount}</strong></article><article className="card"><h3>Approved advances</h3><strong className="metric">{data.advanceTotals.approvedAmount}</strong><p>{data.advanceTotals.remainingBalance} remaining</p></article></div><div className="table-scroll" tabIndex={0}><table><thead><tr><th>Month</th><th>Status</th><th>Employees</th><th>Exceptions</th><th>Gross</th><th>Deductions</th><th>Net</th></tr></thead><tbody>{data.runs.map((r:any,i:number)=><tr key={r.key||i}><td>{r.month}</td><td>{r.status}</td><td>{r.employeeCount}</td><td>{r.exceptionCount}</td><td>{r.gross}</td><td>{r.deductions}</td><td>{r.net}</td></tr>)}</tbody></table></div><h2>Department aggregates</h2><div className="payroll-card-grid">{data.departmentAggregates.map((d:any)=><article className="card" key={d.department}><h3>{d.department}</h3><p>{d.employeeCount} employees · {d.netAmount}</p></article>)}</div></div>;}
