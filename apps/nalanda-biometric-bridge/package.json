{
  "name": "@nalanda/biometric-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "simulate": "tsx src/cli.ts simulate",
    "start": "node dist/agent.js",
    "credentials": "tsx src/credentials.ts"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
